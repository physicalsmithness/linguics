# OS / editor cruft
.DS_Store
Thumbs.db
*.swp
*~
.vscode/
.idea/

# Node / Python (if any tooling is added later)
node_modules/
__pycache__/
*.pyc
.venv/
venv/

# Build outputs (none yet, but in case a backend lands)
dist/
build/
*.log

# Author-chat feedback and intermediate review artifacts.
# These are useful while iterating but are chat-thread output rather than
# project state, so they don't need to live in the public repo.
FEEDBACK_*
AUTHORING_FEEDBACK_*
ARCHITECTURE_FEEDBACK_*
REPLY_TO_*
DECISIONS_FROM_*
feedback_*
Feedback_*

# Wrangler / Cloudflare local state
worker/.wrangler/
worker/node_modules/
.dev.vars
