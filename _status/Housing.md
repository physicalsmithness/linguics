seat: Housing
project: Linguics
updated: 2026-08-05
waiting: Smith push r134 + look. NEW: a miss now names the learner's OWN word back (guess 'background' for fondo -> 'You said background - that's sottofondo / retaggio / sfondo'). Also still to check: the jump (r133) and the labelled right answer (r133). OPEN: the vocab result panel may not be carrying the r119 redesign - Smith says it looks pre-redesign. Scrubber + unified filter unbuilt.
needs_from_smith: external-action
blocked_by:
claude_can_verify: push + deploys only Smith can do; the pronoun-tile scroll and cold-load timing only Smith's browser can confirm. Everything in r124 was measured old-engine-vs-new over the whole bank and is pinned in selftest.
summary: v14 WITHDRAWN by the incoming Housing chat 2026-08-02. v14 said the r64-r90 stack was unpushed - FALSE: commit c57e531 (2026-08-02 19:13) carries skillCount at app.js:2316 + the menu injection at translation_marker.js:508, and origin/main==HEAD; its own reported 56k-token live call proves the new client was already deployed. The breadth regression traces to v9 (menu 136 aggregates -> 581 real ids WITH descriptions = 188,749 chars / ~47k tokens per mark); v5 is live evidence that breadth WORKED at 136 (seven markpoints, all Right). So strengthening the breadth WORDING was the wrong remedy. Two Architecture orders were never discharged: v6 en_it production-vocab (the 581 menu excludes vocabulary + the hard bucket_context rule = vocab markpoints are STRUCTURALLY impossible on en_it, not a model-obedience problem) and v9 prompt-cache (bucket_context sits AFTER the per-item block in buildUserMessage, so there is no cacheable prefix and the menu is paid in full every call, ~1.6c/mark). SHIPPED r91: menu is now a runtime parameter - LL.markerMenuMode() / setMarkerMenuMode(), six modes (full/labels/aggregates/agg_labels/retrieved/none), floor preserved in every mode - plus housing/bench.html, a repeatable same-item/same-answer bench with a persisted run log and 'Run every menu mode'. Both client-side, so a PUSH reaches them with NO wrangler deploy: the menu hypothesis is falsifiable against the currently deployed worker. Incidental: all 875 translation items key on external_id not id; pulse.js:127 already falls back correctly. || PRIOR (2026-07-23): crosstopic v12/v13 triaged (Claude 2026-07-23). Smith's live '0/3' mark: faults 1 (X/N header) + 2 (greens absent) are NOT panel bugs - r70 already surfaces skillCount X/N and renderResult shows every hit; the greens are the WORKER not firing breadth on the deployed path (stub is honest/no-verdicts since r83, so it was the worker). Strengthened the worker BREADTH rule (required, not optional). Fault 3 (placement double-penalty): WORKER rule 14 added (correctly-placed wrong-form clitic = placement HIT + form MISS only). Fault 4/v13 (help=vocab miss, grammar credited): ALREADY the Housing behaviour - appendVocabHelpEvents fires the vocabulary.<lemma>.translation miss (learner_declared), grammar credited separately; looked like a hard Wrong only because it was 1-of-3 with no greens. NO housing code change (r70 + help-miss handle it); worker rules 14+breadth need wrangler deploy. Appended crosstopic v14. NEEDS r90 push + worker deploy. PRIOR: r90 (Claude 2026-07-23) RETRIEVAL_HELP shipped (affordance v1 + credit-only marking v2/v3, was fully open). MARKING (grammar_engine.js): markGrammar computes creditOnly = mp.credit_only===true OR (provenance lemma_retrieval_pilot AND bucket !startsWith vocabulary.); a credit-only markpoint records hit/partial but is SKIPPED (no event, no overall) on miss/not_attempted -> the FORMATION markpoint is silent on a wrong verb OR wrong form, the VOCAB markpoint carries the miss. Verified on pres_lr_parlare_1pl_01 (parliamo=both hit; diciamo=vocab miss only; parlamo=vocab hit, no formation miss). DISPLAY (app.js): buildRetrievalHelpBar - prominent 'Don't know the Italian for X? [Tap to reveal]' bar under the blank for pilot items (replaces the hidden vocab button); reveal records the vocab miss via vocabHelpsUsed. CSS .retrieval-help-bar (blue, per pilot_preview mock). Built BOTH the credit_only flag (future) + the provenance auto-rule (pilot works now). node --check clean. Appended thread v4 (Status->SHIPPED). NEEDS r90 PUSH. PRIOR: r89 (Claude 2026-07-23) STRESS MATRIX RED + DRILL SCORE HIDE + ACCENT TRANSLATION (Smith live). (1) confusion matrix (buildSessionStressMatrix + buildStressConfusionMatrix): off-diagonal ERROR cells now shade RED (rgba(176,48,48,a)), correct diagonal GREEN (rgba(46,125,79,a)) - was uniform olive so wrong didn't look wrong. (2) 'massive one' = the big N/M renderResult score, now HIDDEN on all four drills (renderResult opts.hideScore; gender/spelling/accent/stress pass it) - the green/red highlight + session panel convey it. (3) accent drill shows the tested word's TRANSLATION under the prompt (from the correct choice, accent-folded; same for all variants so no leak). node --check clean. r88 (Claude 2026-07-23) HOVER-KEEPS-REVEAL + DRILL RUNNING-TOTAL. (1) .mcq-choice:hover repainted bg->cream + border->accent, wiping the post-mark green/red; added .correct-pick/.gc-correct/.ms-correct:hover (green) / wrong (red) / ms-missed (amber) rules keeping the reveal + killing the hover box-shadow. (2) drill stats reset every deck-build; added a persisted 'keep a running total' checkbox (drillStatsKeep + per-type localStorage); resetDrillSession restores per-type when on, recordDrillAttempt persists; labels flip to 'running total'. Verified keep-logic harness. NEEDS r89 PUSH. PRIOR: r87 (Claude 2026-07-23) NICER STRESS CONTEXT (Smith: 'present 3pl of X' less nice than 'as in loro abbracciano'). 536 items carry an authored stress_meta.context like 'present 3pl of essere' embedded in q.prompt. NOW: prompt shows the BARE word (sm.word, or q.prompt split on the em-dash); niceStressContext(sm) reformats 'present {person} of {lemma}' -> "as in 'loro sono'" via PERSON_LABELS. Verified on the 6 real samples (essere->loro sono, fare->loro fanno, ...). Base-verb meaning already shows on the r86 translation line (sm.lemma lookup). The CONJUGATED gloss ('- they are') needs conjugation data the items don't carry, and the 536 are mostly IRREGULAR high-freq verbs so a 'they'+stem heuristic is unsafe -> FLAGGED to the stress pipeline (Code/StressAuthor) to author it if wanted. node --check clean. NEEDS r87 PUSH. PRIOR: r86 (Claude 2026-07-23) STRESS DRILL + MCQ REVEAL (Smith live, 4 tweaks). (1) GREY non-stressed text: .stress-choice .gc-label -> #968e7f; the bold/underline/apostrophe stressed syllable stays full ink so it pops. (2) TRANSLATION during the question looks up by base sm.lemma FIRST then surface word/prompt (catches inflected forms the old sm.word-only lookup missed); shows under the prompt, stays on the card through the answer. (3) 'correct was ...' was UNSTRESSED (r79 stripped ** which also removed the stress marking): dropped 'correct was X' from buildMcqResult summary (now just Right/Wrong); the correct stress choice now HIGHLIGHTS GREEN - and its CSS was MISSING (.stress-choice.gc-correct/.gc-wrong had no rule, only gender/spelling), so I added it -> the green button shows the correctly-marked stress. (4) GENERAL MCQ reveals on the choices: commitResult adds correct-pick(green)/wrong-pick(red) (was relying on the text below); .mcq-choice.correct-pick/.wrong-pick CSS added. buildMcqResult summary change is shared by grammar-MCQ/error_id/stress - all three now have visible button reveals. node --check clean, css balanced. NEEDS r86 PUSH. PRIOR: r85 (Claude 2026-07-23) CATEGORY PICKER -> MULTI-SELECT (task 13 / vocab_category_picker v1 last piece). Entry-screen picker was single-select (click replaced). Now chips toggle into v.catSel; catIds=UNION of selected, catLabel=joined; removable 'Selected: [x]' pills; 'All categories' clears; parent+sub active per set; saved/legacy single picks reconstruct on load. Session side unchanged (startVocabSession already fed v.catIds union -> vocabFilter.themes, OR'd). VERIFIED node --check + css balanced + toggle harness (Food+Colours union; remove -> back to All). Earlier picker faults already shipped. Appended vocab_category_picker v2. NEEDS r85 PUSH. PRIOR: r84 (Claude 2026-07-23) DRILL-WORD RENAME (drill_into_reach v4: 'drill' overloaded for filter-toggle AND navigation). The stats-aside FILTER chip now speaks filter: 'Drilled into'->'Filtered to'; step-out 'out/Drill out one level'->'widen/Remove one level of this filter (or click the highlighted box again)'; undo+x titles drop 'drill'->'clear this filter'; entry part-picker 'drill in'->'see inside'. LEFT: analysis navigation language + drill-MODE names (Spelling/Accent/Stress/Gender ARE drills) + freq-dot 'filter this thousand' + internal class/fn names. node --check clean. Appended drill_into_reach v5. NEEDS r84 PUSH. PRIOR: r83 (Claude 2026-07-23) common_errors WIRED (crosstopic v11: 476 authored entries read NOWHERE). STUB (translation_stub.js): matchedCommonError(exact norm / accent-folded / jaccard>=0.9 vs item.common_errors) + knownErrorResult; markTranslationStub now returns a 'known common mistake' (names the note; 0/0 not_scored) instead of PRAISING a near-correct error ('gatti nero') as 'roughly right' word-overlap. Harness: 'gatti nero' FLAGS, correct 'gatti neri' does NOT misfire. WORKER (worker/src/index.ts): common_errors added to buildUserMessage item (was posted whole already) + RULE 13 (known wrong variants -> miss + name, never accept). WORKER NEEDS WRANGLER DEPLOY (like 11/12). node --check stub/app clean; worker braces balanced. Appended crosstopic v12. NEEDS r83 PUSH + wrangler deploy. PRIOR: r82 (Claude 2026-07-23) multi_select QTYPE BUILT (top of the open-work map; unblocks live_round2 ask-4 AND PronounAuthor's 6 op_ord_*_ms_01 order-battery items). type:multi_select = toggleable checkbox choices (1-N keys + click), explicit Mark, reveal green/red/amber (selected-correct/selected-wrong/missed-correct). Self-contained early branch in renderGrammar (q at top -> renderMultiSelectCard(q,host); return) to avoid the fragile shared MCQ/error_id path. buildMultiSelectResult: ruling(i) credit=max(0,(correctPicks-wrongPicks)/totalCorrect); ruling(ii) one item markpoint on q.subtopic + per-choice Rev-27 cross-credit from choices[i].buckets (skill by ANY selected correct = hit; only-in-missed = miss; hit wins; suppress_display) + optional choices[i].misconception_id via r68 path. Schema: choices[{text,correct,buckets,note}] + correct_indices + subtopic. VERIFIED node --check + css balanced + scorer harness on real op_ord_dative_ms_01 ([0,1,2]->1.0, +1wrong->0.67, all->0, [0]->0.33, cross-credit buckets aggregate right). Appended live_round2 v4 + a Housing note to the order-battery thread. NEEDS r82 PUSH. PRIOR: AUDIT 2026-07-23 (Claude): open-threads pass done + wrote EdTechOverview_Housing_per_user_ai_cost_cap.md v1 (recording live r81; enforcement recommended worker-side; 5 decisions for EdTech/Code). OPEN HOUSING WORK MAP (verified vs disk r81): (1) multi_select qtype renderer - UNBUILT, HIGHEST LEVERAGE (blocks live_round2 ask-4 AND PronounAuthor's 6 op_ord_*_ms_01 items authored as type:multi_select); (2) common_errors (476 data entries) authored but wired NOWHERE (crosstopic v11: read item.common_errors in the stub + pass to worker); (3) drill in/out RENAME - the FILTER toggle still says 'Drilled into/Drill out', should read apply/remove filter (drill_into_reach v4; nav exit already renamed 'Back to practice'); (4) vocab category picker is SINGLE-select, wants multi-select removable chips (vocab_category_picker v-latest / task 13); (5) retrieval_help_affordance FULLY OPEN - data present (retrieval_help_lemma) but no housing affordance + no credit-only marking; (6) bespoke grids A6 tense-choice confusion matrix + A7 piacere = literal placeholderSection (~app.js 9188); (7) apostrophe allow-list not retired so da/di (grave) still un-typeable (AccentAuthor_Housing_apostrophe_input, half-met); (8) STR/ACC report AXES (accent_stress §1) not built. DONE-confirmed by audit: cue_notation, residue+showResiduePrompt banner, grammar_pulse_strip question-counts, join-key event model (attempt_id+item_id+person+FK), translation_stub_honest. PRIOR: r81 (Claude 2026-07-23) AI COST RECORDED PER USER + CAP SWITCHED OFF (Smith redirect: 'record the actual cost, let me set per-user caps; don't turn the cap on yet'). Cost was already COMPUTED (worker returns cost_usd; worker self-refuses any call >$0.03) and SHOWN (per-call costLine + footer session total in localStorage linguics_session_cost_usd) but NOT persisted per attempt, so there was no cost-per-user in the data. NOW: doMark attaches result.ai_cost={usd,input_tokens,output_tokens,model} on each successful markTranslationLive; store.js persists attempt.ai_cost; pulse.js lifts ai_cost_usd/ai_input_tokens/ai_output_tokens/ai_model as TOP-LEVEL keys -> one per sheet row (blank on non-AI rows). SUM ai_cost_usd grouped by identity = cost per user. r80's per-browser count cap DISABLED (AI_TRANSLATION_FREE_LIMIT=0; aiOverLimit now also requires limit>0; the .ai-upsell mechanism kept but dormant, set >0 to arm). Per-USER cap ENFORCEMENT is worker-side (it already meters per-call; per-identity cumulative cap is the natural extension) - flagged, not built. VERIFIED node --check app/store/pulse + pulse-lift harness (AI row populates cost keys, grammar row blank). METRIC: recording actual $ (ground truth, varies by tokens) not just call-count; Smith can cap by either later. NEEDS r81 PUSH. PRIOR: r80 (Claude, back after no-credits, 2026-07-23) AI-COST CAP + UPSELL - Smith held item #1, now built. Only the AI TRANSLATION check is capped; grammar/vocab/drills are locally marked and stay UNLIMITED. Hard-coded AI_TRANSLATION_FREE_LIMIT=20; aiMarksUsed()/bumpAiMarksUsed() persist a per-browser count in localStorage(linguics_ai_marks_used). doMark: AI path gated on markerUrl && used<limit; bumps on each SUCCESSFUL markTranslationLive; over the limit -> skip the worker, fall back to markTranslationStub (app still works) + append an .ai-upsell banner with a 'Register interest' CTA that fires track('subscribe_interest') so demand is measurable (no fake checkout). No worker configured -> no upsell (correct). SOFT client-side cap (bypassable by clearing localStorage) - real per-account enforcement needs the WORKER to meter by identity (flagged to Smith/EdTech). CSS .ai-upsell*. Verified node --check clean + css balanced + gate arithmetic (used 0/19 allow, 20/21 upsell). Limit + copy trivially tunable. Co-editing seat with QoderWork; _status is the shared channel. NEEDS r80 PUSH. PRIOR: r79 (QoderWork 2026-07-23) STRESS DRILL UX OVERHAUL (Smith's live-session feedback). (1) RESULT TEXT FIX: stress commit now strips ** bold markers from raw_response, summary, and markpoint evidence/expected before display (was showing literal stars); renderResult gains opts.youWroteLabel — stress passes "You tapped:" instead of "You wrote:" (it's a tap drill, not free-text). (2) TRANSLATION SHOWN: prompt word looked up in vocabEntries by lemma; if found, English translation rendered as .stress-translation (italic, under the prompt). (3) PROMPT ACCENT STRIPPED: stripWrittenAccent() removes written accents from the prompt word so a trailing accent doesn't give away tronca answers (e.g. dilagò → dilago). (4) CHOICES IN BORDERED BOXES: .stress-choice restyled — 1.5px border, 8px radius, padding, hover border+shadow, active inset shadow; looks tappable. (5) TINY CORNER NUMBERS: .gc-num repositioned absolute top-left, 9px, faint, opacity .6 — no longer competes with the stress marking. (6) STRESS MARKING MODE TOGGLE: new stressMarkMode variable + STRESS_MARK_MODES array (bold/underline/bold+underline/' before syllable); 4 chips in filter bar (stress mode only); appendStressMarked() renders choices per mode — apostrophe mode inserts ' before the stressed syllable via .stress-apo-mark span; CSS classes .stress-mark-underline/.stress-mark-both/.stress-mark-apostrophe on the card control the visual. (7) LEGEND NUMBERS REMOVED: was "1=tronca · 2=piana..." which conflicted with choice display numbers; now just names "tronca (last) · piana (2nd-last) · sdrucciola (3rd-last) · bisdrucciola (4th-last)". (8) EXPLANATION + RULE HINT: after the formulaic explanation, a per-class rule-of-thumb is appended (.stress-rule-hint) — tronca: stressed-vowel endings/short words; piana: the default; sdrucciola: -ico/-issimo/-ano suffixes; bisdrucciola: 3pl -are verbs. Gives the WHY, not just the WHAT. CSS: .stress-translation, .stress-apo-mark, .stress-rule-hint. Build label r78→r79. VERIFIED: node --check clean (app/store/pulse). NEEDS PUSH. PRIOR: r78 (QoderWork 2026-07-23) THREE CHANGES. (1) ENTRY-SCREEN STRESS CSS: chat 1 was terminated before adding CSS for the entry-screen stress example (.entry-stress-example/.entry-stress-variant/.entry-stress-or); now added — the capitano variant row renders with bold+underline stressed syllable, flex-wrap, italic 'or' separators. (2) ACCENT DRILL ON ENTRY SCREEN: accent drill now has its own entry-screen section (chip + description), same pattern as gender/spelling/stress; accentDrill added to defaultEntrySel vocab state; mutual exclusion across all four drills; startVocabSession wires accentDrill + buildAccentDeck(); track includes accent_drill. All four drills (gender, spelling, accent, stress) now visible on the front page. (3) DRILL SESSION PANEL: new right-hand breakdown in #vocab-axes-host during all four drill modes — replaces the vocab axes (which show frequency/gender/themes, irrelevant to drills) with a live session summary: title + 'N attempted · M correct' (raw counts, no thresholds) + per-category table with olive-shade bars (rgba(61,74,28,pct)) + for stress a live 4×4 confusion matrix (true\tapped, global-max olive shade, diagonal outlined). Session accumulator (drillSession) resets on deck build (buildSpellingDeck/buildAccentDeck/buildStressDeck) or gender mode entry; recordDrillAttempt fires on each commit with category + correctness (+ stress matrix coords); renderDrillSessionPanel() called after every commit (live update) and on initial render. CSS: .drill-session-panel/.drill-session-table/.ds-cat-*/.drill-session-matrix. Smith: 'immediately to the right of the action, the breakdown of what you've done.' VERIFIED: node --check clean (app/store/pulse). NEEDS PUSH. PRIOR: r77 (QoderWork 2026-07-23) STRESS DRILL SHIPPED (StressAuthor's 20,005 items, Architecture_Housing_accent_stress_and_new_qtypes.md §2). LOAD PATH: data/grammar_questions_stress.json added to the tail singleton Promise.all, but kept in its OWN module-level stressQuestions array — NOT merged into grammarQuestions (their choices embed **bold** syllable markers the general grammar MCQ path would render literally). Loader returns a separate `stress` key. UI: 5th mode chip "Stress drill" in filter-mode-row; vocabFilter.stressDrill flag; known-words direction filter applies; compact info bar in stress mode. buildStressDeck() (mcq + >=2 choices, knownDir via practisedLemmas on stress_meta.word) + renderStressDrillCard(): tap-the-stressed-syllable — each choice shows the word with one candidate syllable emboldened, rendered **syl**-><strong> via DOM-safe appendBoldedText (no innerHTML); shuffled display positions (no position bias); ambient class legend "1=last(tronca)·2=2nd-last(piana)·3=3rd-last(sdrucciola)·4=4th-last(bisdrucciola)" shown but never reveals THIS word's class (that's the answer); index-scored via buildMcqResult (choice_tags misconception firing inherited r68); keyboard 1-N + click; explanation reveal. result.stress block {true_pos, answered_pos, syllable_count, stress_class, word} attached on commit — answered_pos = original choice index + 1 (choices[i] bolds the syllable i+1 from the END), true_pos = stress_meta.stress_pos. PERSISTENCE: store.js recordAttempt persists result.stress -> attempt.stress_attempt (same pattern as tense_meta/form_meta). PULSE: pulse.js buildAttemptPayload lifts stress_true_pos / stress_answered_pos / stress_syllable_count as top-level keys (estate script sweeps them into extra_json; reconstitutes the 4x4 row-normalised matrix in the workbook). STR STRIP BOX: wired to phonology.stress.* buckets (was an explicit placeholder) — paintStripBase feeds byBox.str, pulseStripFlash fires it green/red. 4x4 CONFUSION MATRIX REPORT: buildStressConfusionMatrix() added to the Analysis view (after person×class grid) — rows=true position, cols=tapped position, diagonal=correct; raw counts primary + row-normalised % secondary (Smith: raw counts, no thresholds); shading = ONE global max across all 16 cells, olive rgba(61,74,28), constant font colour never flips (Smith heatmap prefs); diagonal outlined. CSS: .stress-legend/.stress-choice strong (bold+underline)/.stress-confusion-diag/.stress-confusion-pct. Build label r76->r77. VERIFIED: node --check clean (app/store/pulse); data harness 20,005 items 0 failures (answer_index+1==stress_pos; choices[i] bolds syllable i+1 from end; option count==min(syllable_count,4); exactly one bold marker/choice; strip reproduces word); store+pulse harness (stress_attempt persists + survives localStorage round-trip + 3 pulse keys lifted). DATA NOTE: 2,405 accent_final wordform items (accent_cue=true, mechanism=orthographic) show the written accent in the PROMPT (that's the cue being tested) and strip it in the choices — by design, not a leak. ALSO (same push): FEEDBACK NAME AUTO-POPULATE — the feedback widget's name field now pre-fills with the signed-in display_name (the name entered at the sign-in gate). feedback.js is WEB_KIT standard (do not fork), so a small watcher script in index.html observes the panel and fills the freshly-rebuilt #wk-fb-name from smithics_fields_identity_v1 on every open (reads fresh, fills only when empty, trims, fails soft). Harness-verified (mount fill + open-reset refill + no-clobber + signed-out no-op). NEEDS PUSH. PRIOR: r76 (QoderWork 2026-07-22) HOTFIX — grammar card render was DEAD (Smith screenshot: empty question area + empty live-stats, footer label r70). Root cause: r75's error-ID branch wrapped `const promptEl = renderPromptElement(...)` inside the else block, but the free-text branch still referenced `promptEl` at the vocab-help insertion (`card.insertBefore(helpBar, promptEl.nextSibling)`), now out of scope -> ReferenceError on every free-text item with vocab_help, thrown before the card was appended, halting the whole render (live-stats never populated). FIX: hoisted `let promptEl = null;` to function scope. node --check clean. Build label bumped 2026-07-21-r70 -> 2026-07-22-r76 so Smith can confirm the fix loaded. PRIOR: r75 (QoderWork 2026-07-22) FIVE CHANGES. (1) SESSION SCORE REWORK: removed 70% correct/incorrect threshold; now raw marks_awarded/marks_possible accumulation; chip shades from white to deep olive rgba(61,74,28) by percentage; text flips white past 55%; reset button kept; CSS transition 0.4s. Smith: "no judgments, no thresholds, just keeping count." (2) KNOWN-WORDS-ONLY DRILL FILTER: new vocabFilter.knownOnly checkbox appears in all drill modes (gender/accent/spelling); practisedLemmas() builds a cached Set of all lemmas with ≥1 vocab event (either direction); gender drill deck filters to practised nouns; accent drill filters items whose correct-answer word (accent-stripped) matches a practised lemma. Smith: "a tick for just words I know." (3) IDENTITY STRIP: #identity-box moved from absolute inside header to a full-width thin strip ABOVE the header (insertBefore); CSS: flex, justify-content:flex-end, 12px, border-bottom. (4) RESIDUE SOFT SECOND-CHANCE: showResiduePrompt() intercepts residue-engine misses before commitResult; gentle banner "You had the right answer, plus extra word(s) (X). Want to trim it?" with "Let me trim it" (retry, refocus+select input) and "Mark as is" (commit the miss); fires at most once per item; Esc=retry, Enter=mark. (5) ERROR-ID QTYPE RENDERER: new type "error_id" in grammar cards; prompt sentence rendered as inline tappable word buttons (.errorid-words/.errorid-word); tap=immediate mark; keyboard 1-N selects, Enter marks; index-scored via buildMcqResult (choices synthesised from prompt words, answer_index=error_index); post-mark highlighting (.correct-pick/.wrong-pick/.reveal-correct); correction field surfaced in explanation; choice_tags misconception firing inherited. NO DATA YET — overregularisation_distractors_v1.json (216 records) is raw material for an authoring pass; renderer is live and waiting. ALSO: gender translation-credit rule confirmed ALREADY BUILT (markVocab lines 4716-4772: gender fires only on unambiguous signal, bare lemma leaves gender silent). Status note was stale. PRIOR: r74 (QoderWork 2026-07-22) AI MARKER QUALITY + PER-WORD COLOURING + VOCAB CREDIT FIX. Worker prompt (worker/src/index.ts): added rule 11 ANTI-HALLUCINATION GUARD (only report accent errors when the learner's form VISIBLY DIFFERS from the correct form in accent marks; if spelling matches reference exactly, do NOT report; when uncertain, stay silent) — fixes the "torneremmo" false accent accusation. Added rule 12 VOCABULARY vs GRAMMAR distinction (wrong word = vocabulary bucket, NOT gender/agreement; only fire gender bucket when the RIGHT word has wrong grammatical marking) — fixes "maglia" for "maglione" being tagged as a gender error instead of vocab. NEEDS WRANGLER DEPLOY. Housing (app.js + style.css): "You wrote" line now does PER-WORD colouring — each word checked against reference answers; words found in references render green (.yw-right), words not found render pink with soft red background (.yw-wrong). Falls back to single-colour when no references available. CSS: .yw-word/.yw-right/.yw-wrong added. VOCAB CREDIT FIX (translation_marker.js): IT→EN vocab injection now uses accent-stripped 3-char stem matching + startsWith containment, so conjugated forms (dirò→dire, vedo→vedere, glie→gli) correctly inject vocab recognition buckets into bucket_context. Previously only exact lemma or 4-char prefix matched, missing most conjugated verbs. TOKEN NOTE: 55749 input tokens is real — the 581-entry cross-topic menu (translation_marker_bucket_menu.json) is ~13k tokens alone, plus per-item vocab injections for IT→EN. Architecture may want to trim the menu or use prompt caching. PRIOR: r73 (QoderWork 2026-07-22) ACCENT DRILL SHIPPED. Variant pipeline run: tools/expand_accent_seeds.js regenerated data/grammar_questions_accent.json (276 items: 85 pick_bare, 33 pick_context, 158 judge; flat array format matching grammar_questions_*.json convention). Loader wired: accent file added to tail Promise.all singleton loads, merged into grammar array after per-topic flatten. UI built: 4th mode chip "Accent drill" in filter-mode-row; buildAccentDeck() filters grammarQuestions by subtopic "accent.*"; renderAccentDrillCard() with shuffled choice positions (no position bias), neutral meta header (no class reveal), keyboard 1-N + click, correct/wrong highlight, explanation reveal, buildMcqResult scoring. vocabFilter.accentDrill flag added. Filter bar shows compact info bar in accent mode. PRIOR: r72 (QoderWork 2026-07-22) EIGHT CHANGES from Smith's live-session feedback. (1) SPELLING DRILL SHUFFLE: choices now shuffled at render time (order array + correctDisplayIdx mapping) so the correct answer isn't always position 2. (2) SPELLING DRILL HEADER: removed error-class label from per-card meta (was "Spelling drill · Digraphs (gl/gn/sc)" — gave away the pattern); now just "Spelling drill". (3) GENDER AXES TICKS/CROSSES: replaced confusing "avg 20%" text with ✓/✗ symbols per touched lemma (up to 12 shown); cell background still colours by mastery. (4) GENDER DRILL COMPACT: choices now 2-col CSS grid (7 options in 4 rows not 7); font 13px, tighter padding; declension feedback moved ABOVE actions row (was below Next button, invisible); .gd-declension now 16px bold with 4px accent left-border. (5) MID-SESSION MODE SWITCHER: filter bar now has a .filter-mode-row with three pill chips (Words / Gender drill / Spelling drill) allowing mode switch without returning to entry screen. (6) FILTER BAR GUARDS: spelling drill shows compact info bar (no Direction/Restrict/Sub-band); gender drill hides Direction selector (irrelevant to classifier). (7) DEAD CODE REMOVAL: rgbToHex, hexToRgb, paletteAsJson, buildNumberInput, buildColourInput all removed (unused since palette-tuning row retired Smith 2026-07-20/21). (8) CUE NOTATION RENDERER (Architecture_Housing_cue_notation_renderer v1+v2): "Use X" now fires ONLY for single Italian citation forms (ends in vowel, not in English blocklist); multi-word cues get "Meaning:"; English triggers get no prefix; auxiliary-fill items get "Verb:" (context, not operand). Resolves ~138 items rendering "Use usually" etc. ALSO: spelling drill resultHost moved above actions (feedback visible immediately). ALSO: filter-bar sticky top fixed to 110px desktop / 78px mobile (was top:0, tucked behind header). PRIOR: r71 (QoderWork 2026-07-22) SIX CHANGES. (1) UNCATEGORISED NOUN CLEANUP: nounClassFallback() helper classifies by ending (-o/m->regular_o_masc, -a/f->regular_a_fem, -e->e_ambiguous); labels: unspecified_m->"Masculine", unspecified_f->"Feminine", unspecified->"—". All three fallback sites updated. (2) SPELLING DRILL: new vocab-strand drill mode using SpellingAuthor's 141 MCQ items. Entry-screen toggle + 8 category chips (doubling, digraph, c_g_softening, qu_cu_cqu, silent_h, apostrophe_elision, capitalization, vowel_confusion). buildSpellingDeck() + renderSpellingDrillCard() with numbered MCQ choices, keyboard 1-N, correct/wrong highlight, explanation reveal, buildMcqResult scoring (choice_tags misconception firing inherited r68). Mutually exclusive with gender drill. CSS .spelling-choices/.spelling-choice. (3) OPEN ANALYSIS + PROFICIENT NOTE right-aligned: .live-controls now flex-column align-items:flex-end; .analysis-open-link width:auto text-align:right; .proficient-note margin/border removed. (4) 9-BOX PULSE STRIP: replaced old 3x3 cell grids + slivers + mini boxes with 9 full-width boxes (5 vocab 1k-5k + ACC + STR + GEN + SP). Each box: gradient background by mastery (linear-gradient green alpha), individual flash firing on relevant events, vocab boxes click-through to vocab strand. Strip appended after h3+controls so "Live stats" heading sits above. .live-header flex-wrap + .pulse-strip flex-basis:100% for own row. Removed dead stripCellForRank/STRIP_CELL_SPAN/ACCENTED_STRIP_RE. (5) STUCK HELP ABOVE ACCENTS: translation card helpBar now inserted before buildAccentBar (was after). (6) DIRECTION STICKY: .filter-bar position:sticky top:0 z-index:35. NOTE: housing todo items 3/4/12/13 still unidentified (no numbered list found in queue; Smith said "carry on" when asked). NEEDS PUSH. PRIOR r70 TRANSLATION SCORE = SKILLS. renderResult header hard-coded overall.marks_awarded/possible; translation's overall is NORMALISED to 0-1 so it always showed 1/1, hiding that N skills fired. Added opts.skillCount (passed ONLY from the translation mark path, line ~1905): header now shows hits / displayed-markpoint-count (e.g. 8/8), = Architecture_Housing_translation_crosstopic_marking's 'X / count'. Grammar/vocab/gender untouched (single skill, stay 1/1). Verified harness (8 hits +1 suppressed -> 8/8; 2hit/1partial/1miss -> 2/4; grammar -> 1/1). NOTE r64-r70 all unpushed. NEEDS PUSH. PRIOR: r69 GENDER DRILL SHIPPED (Smith: 'build the gender driller'; to Architecture_Housing_gender_plural_drill v3, the Smith-driven REDESIGN = uniform fixed-option classifier). Bare noun + FIXED 7-option gender-behaviour list every question (click or 1-7, one-tap), index-scored. Correct class DERIVED via nounGenderClass(entry): prefers an explicit gender_class tag (upgrades free when Vocab populates it - Smith 'imagine the dbase will be fixed'), else interim mf->3 / m+(-a pl,-o lemma)->5 / m->1 / f->2. Wrong pick fires gender_class.thought_<key> (proposed) via the r68 MCQ-misconception path + marks a miss on the noun gender bucket. Deck = clean-gendered nouns (excludes the ~1030 'ambiguous' junk), interesting-classes-focused + rank%20 m/f baseline. VERIFIED node --check + harness (casa->2, libro->1, braccio/uovo->5, mano->2; mark+misconception correct). KNOWN interim gaps (need Vocab's gender_class tag): class 3 either (giornalista tagged m not mf), 4 depends-meaning, 6 eco, 7 two-plurals - NOT derivable from current data; did NOT use the homograph split heuristic (junk). Appended v4 to the gender thread (Next: Vocab gender_class tag + MisconceptionAnalyst canonical ids + NounAuthor class-set confirm). STILL HELD (Smith brief): AI-cost cap + subscription upsell; gender translation-credit rule in VOCAB (en_it: never required, credit when written). NEEDS r69 PUSH. PRIOR: r68 MCQ MISCONCEPTION FIRING (multi-chat blocker; Smith relayed 'lots of chats asking'). Free-text guards fired misconceptions since r10, but buildMcqResult never populated result.misconceptions, so a wrong MCQ pick recorded NOTHING. Found choice_tags already in data (array parallel to choices, entry null|{class,misconception,proposed}; 141/338 mcq items, all orthography). buildMcqResult now fires {id,bucket,evidence,proposed} on a WRONG tagged pick -> recordAttempt persists attempt.misconception_hits -> flows into the misconception analysis end to end. Marking UNCHANGED (attribution only, r10 semantics). Bucket = item markpoints[0].bucket (choice_tags has none). VERIFIED harness on the real capello item (both wrong picks fire gemination.geminate_simplified; correct pick clean; untagged/no-tags safe). Wrote inter_chat/Architecture_Housing_mcq_choice_misconceptions.md v1 (OPEN, Next: Architecture): asks them to RATIFY the choice_tags schema + rule bucket/class attribution - that's the piece the waiting chats need from ARCHITECTURE; the reader itself is LIVE at r68 so tagging fires the moment it's pushed. HELD (Smith brief, mid-turn, NOT built): (1) AI-cost cap + subscription upsell on translation AI marking (hard-coded free limit, then offer subscription after they've seen quality); (2) gender translation-credit rule scoped to VOCAB en_it (Smith clarified): gender NOT required, credit when M/F or gendered article written, always evaluated - independent of the drill, buildable in markVocab now; (3) gender DRILL wants ~7 category options incl. masc-sg/fem-pl - but DATA BLOCKS IT: noun `gender` field holds only m(4875)/f(3810)/mf(104, small+noisy)/ambiguous(1030, JUNK: 'the','of','Roma',thin gap-fill - exclude). The masc-sg/fem-pl set (uovo->uova) is UNTAGGED (stored as m + irregular plural; a famous closed ~dozen). Needs the VOCAB chat to add a clean gender-category field + bin the ambiguous junk before the full drill; can build m/f/either v1 now; (4) accent/stress drills NOT ready per Smith - hold. NEEDS r68 PUSH. PRIOR: r67 STREAK + HOME. Streak grid (footer) nudged down (margin-top 4px) + a touch smaller (cells 7->6px, label 11->10px) per Smith - BLIND CSS, confirm on push. BACK-TO-FRONT-PAGE: the header brand (logo+wordmark) is now click-to-showEntry (LL.showEntry already existed); it's in the sticky header so it's on every practice screen. DRILL-ENTRIES finding (Smith's 'space for Gender/Accent/Stress'): only GENDER drill actually exists (entry-screen genderDrill option, nouns-only focus); ACC is a PASSIVE tracking light (fires on accent verdicts during normal marking), not a drill mode; the STRESS drill does NOT exist (STR strip light is an explicit placeholder 'waiting for the stress drill... when it exists'). So a 'Special drills' section can host Gender live now, but Accent/Stress need the drills built first - Stress needs a spec of what it even tests. RULING NEEDED: reserve inert 'coming soon' Accent/Stress slots vs hold the section until built. NEEDS r67 PUSH. PRIOR: r66 THEME/GENDER DRILL UX - Smith RETRACTED the 2026-07-20 restriction-focus ask (he meant the DECK narrows to the group, not that the axis hides every other cell). Removed the two restriction-focus continues so ALL theme/gender cells stay visible with the drilled one highlighted (.filter-active); added an isActiveFilter guard so clicking the highlighted cell ALWAYS clears the drill (undo), regardless of expand state. Top 'Back to all' + bottom indicator unchanged (Smith confirmed they read clearly). node --check clean. NEEDS r66 PUSH. PRIOR: r65 VANISH FIX - REAL cause (Smith was right; my r64 flanking-dilution diagnosis was WRONG). The dots that 'flashed then vanished' in the 1000-2000 FOCUSED block were HOMOGRAPH RANK COLLISIONS: 480 ranks estate-wide (175 in 1000-2000) carry >1 entry (largo+toccare @1015, nave+presente @1004, ...), and byRank kept only the FIRST entry per rank, so practising any non-first entry never lit its dot - it flashed (by data-rank) then reverted to untouched (first entry has no events). FIX: byRankAllMap() (rank->ALL entries) + eventsForRank(rank,dir) unions every entry's events chronologically; focused dots, flanking cells and recolourAxisCellsFor all aggregate per-rank now. ALSO fixed a MIX-mode bug: activeDir mapped 'mix'->active-only (hid every passive event); now vocabHeatmapDir() => 'any' for mix/unset. VERIFIED node --check + merge harness (sister practice lights the dot; recency order; single-entry fast path). r64 (recolour-in-place on mark instead of full rebuild; keepAxes on Next) STILL STANDS - correct for perf, just aimed the vanish story at the wrong cells. THEME DRILL diagnosed, NOT yet fixed (task 10): over-threshold theme (colours) expands on click1, filters on click2; once vocabFilter.theme set the axis HIDES every other theme cell (restriction-focus = Smith's own 2026-07-20 ask) -> feels like a new page, heavy border, only the slow 'Back to all' to escape; reclick-to-clear exists but Smith can't find/use it. RULING NEEDED: keep restriction-focus + add a fast one-click clear on the active cell, or drop it (dim others instead). NEEDS r65 PUSH. PRIOR: r64 VOCAB PERF + VANISH FIX (Smith direct). SLOWNESS root cause: the per-answer FULL heatmap rebuild. doMark called renderVocabAxes() (teardown+rebuild of ~990 focused dots + ~20 flanking blocks, each dot an eventsForEntry lookup + click listener) on EVERY mark, and renderVocab() rebuilt it again on EVERY Next. NOW: doMark recolours ONLY touched cells in place via new recolourAxisCellsFor(entry) (focused dot + its flanking cell + gender/theme lemma dots; sets SETTLED bg, not just a flash); renderVocab(keepAxes) + doNext->renderVocab(true) skips the rebuild on advance; full rebuild now only on strand-enter / filter change. VANISH: focused-grid hits already persisted (scrittore: 1 event->1.0->dark green); the 'flashed then vanished' was FLANKING cells - a cell aggregates ~9 lemmas so one hit dilutes to ~0.29, and baseline==yellowStop(0.2) made touched read as untouched (disponibile). FIX: recolour sets the true aggregate AND adds .has-practice (inset green border) to any flank/lemma cell containing practice; also added in renderFlankingStripes so it survives full rebuilds. CLEANUP: removed 'Vocab practice' h2 + the redundant note <p> (dup of card hint + a 'translation only' placeholder); removed the 'Yellow stop' dev tuning input (var kept, default 0.2); lowercased the 2nd 'enter' in the card hint (2 sites). node --check clean, css balanced. WORKER now deployed by Smith (wrangler x3, live ver 9c83f0d4) so r63 cross-topic translation marking is LIVE. DEFERRED vocab work-order (Smith brain-dump, queued, need push+decisions): strip demarcation + align Restrict-to/Sub-band labels; direction sticky top-right + 'translation only' (needs ruling on removing EN->IT/mix option); theme picker w/ family grouping+hover+select-all (check Vocab taxonomy dep); drill-in no-way-back / can't-unclick; drill entries for Gender/Accent/Stress + back-to-front-page (Stress unwired); remove rest of palette-tuning row? NEEDS r64 PUSH. PRIOR: r61 + WORKER FIX - the AI marker IS working; the red box (r61) revealed the real cause: DeepSeek wraps its JSON in a ```json ... ``` markdown fence, and worker/src/index.ts did a BARE JSON.parse(content) -> malformed_json -> housing fell back to the stub. Fixed worker/src/index.ts (~L451): strip the code fence, and as a fallback extract the outermost {...}, before JSON.parse. CRITICAL: this is a WORKER change - needs `wrangler deploy` from the worker/ folder (or the EdTech/worker owner), NOT the GitHub push that deploys the housing site. After the worker redeploys, the real per-skill AI marking fires. r61 surfaced the failure reason (was erased by resultHost reset). r60 stats-pane top pinned + last-10 coloured. r59 re-mark exploit locked. PRIOR: AI TRANSLATION MARKER 'not firing' diagnosed + partly a silent-failure bug of MINE: when a worker URL is set (toggle 'on'), markTranslationLive IS called, but on ANY error the catch wrote a 'Live marker failed' note that the very next `resultHost.innerHTML=""` ERASED before render - so the AI was failing and the learner only ever saw the stub ('real AI marker will give per-skill feedback'), silently. Now the failure reason is CAPTURED (aiError) and shown above the stub result in a red .marker-ai-error box. So on next push+mark, Smith will see the ACTUAL cause (network / HTTP / worker error) - likely a worker deploy or URL issue, which is worker/EdTech-side not Housing code. r60 stats-pane top pinned + last-10 coloured. r59 re-mark exploit locked. PRIOR: live-review batch. r59: RE-MARK EXPLOIT closed - doMark had no `if (marked) return`, so you could change your answer and press Mark again to overwrite a miss with a hit (and the r52 dedupe kept the latest, helping the cheat); now guarded + input goes readOnly on commit; second-chance path unaffected (marked still false during it). r60: STATS-PANE TOP pinned - the vocab pulse strip now mounts in #live-stats .live-header (was top-left of the drill), the last-10 row moved into the header, the whole .live-header is position:sticky so it doesn't vanish on scroll, and the last-10 ticks are coloured (green hit / red miss). r58 hover trims (tree-row description dump + Drill-into-this gone; slivers get leaf context). r57 agreement cells light (non-person sub-aspects get plain fill not empty person barcode). r56 mini-cell sub-aspect labels. PENDING from the pile: identity strip (Smith·Test) above the header line. STICKY/layout wants Smith's eye on push (blind CSS). PRIOR: RESIDUE ENGINE shipped (marking absorb 2/3, extraneous_word_residue v3 GO): single-markpoint non-MCQ answers containing the positive PLUS extra unlicensed words now score a NAMED MISS. residue = norm(answer) MINUS all matching any_phrases (all-matches) MINUS whole prompt tokens (blanks/brackets/parens stripped); harness-verified w/ REAL norm on the thread's table. Multi-markpoint untouched; soft second-chance banner not yet wired. r54 - accent DOCKING (0.5 omission / 0.8 wrong-kind, supersedes never-dock) + quick-start rename 'Work on recent weaknesses'. r53 - Stuck?Help moved under the question right-justified. r52 - spam-to-green exploit closed + hover trimmed. REMAINING absorb: error-ID qtype. Polish: corner streak, segmenter colon, welcome coverage taste. PRIOR: TWO Smith-flagged bugs fixed. (1) SPAM EXPLOIT closed: recencyWeightedCorrectness weighted every attempt, so repeating one question stacked weight past the 9.5 two-for-green floor -> dark green. Now rawEventsFor tags each event with its source item and recencyWeightedCorrectness dedupes to the most recent event per (item,bucket) before weighting, so mastery reflects DISTINCT items. Harness-verified w/ real constants (W_LAST 7/W_PRIOR 3/floor 9.5): spam 1 item x5 -> 0.737 (proficient, NOT dark), 2 distinct items -> 1.000. (Vocab path via eventsForLemmas untouched - events there lack .item so dedupe skips them; follow-up if vocab spam matters.) (2) HOVER trimmed: friendlyOverviewTitle now = label + question-shaped counts only, dropped the full bucket-description paragraph + 'Drill into this' (Smith: 'hover text is mad - either full paragraph or just io'). Person-band slivers already show 'io: X% (n attempts)'. r51 - design structural fold complete. r48 analysis 3-tab. r44 wine. r43 - design STRUCTURAL fold essentially COMPLETE. Welcome-splash now has per-strand analysis links ('full grammar analysis ->' opens Analysis on Grammar tab, 'full vocabulary analysis ->' on Vocabulary) replacing the single 'See your analysis'. Bucket-tree browser DROPPED from the top nav (design: fold into Analysis) - now reachable via a 'Browse the atomised bucket tree ->' link inside the Grammar analysis canvas. Design fold done across r44-r51: wine reskin, logo, analysis 3-tab (wired for real r48), clear-palette default, mobile bottom tab bar, sticky 'Linguics' header, feedback lift, welcome links, bucket-tree fold. REMAINING = marking-side ABSORBS (own round, they change SCORING): residue engine (additive-word named miss, single-markpoint, soft 2nd-chance), error-ID qtype (pick the wrong word, index-scored, over-regularisation distractors), accent DOCKING (0.8 wrong-kind / 0.5 omission, supersedes 0.9-flat). Finer: corner streak on practice screen, GEN/STR/ACC condensed rail. r50 sticky header. r49 mobile tabs. r48 analysis 3-tab. r44 wine. r43 - HEADER now STICKY (position:sticky top:0) so 'Linguics' (logo+wordmark+nav) is permanently there when scrolling on desktop AND in analysis (Smith: 'need linguics permanently there'). Feedback pill: already position:fixed bottom-right in feedback.js (NOT forked) so it stays on-screen on desktop; lifted above the mobile tab bar via CSS override (was going to overlap). If Smith still sees feedback off-screen in a specific view, need that view. r49 - MOBILE BOTTOM TAB BAR. NOTE live is r44+ (Smith pushing); STOP parroting stale live numbers. r48 analysis 3-tab wired for real, r45 logo, r44 wine reskin. r43 - MOBILE BOTTOM TAB BAR shipped (design overhaul): fixed-bottom Grammar/Translate/Vocab/Analysis on phones (<=600px), top strand-nav hidden there, wired to showStrand/showCoverage with active-state sync. NOTE Smith corrected me: live is r44 not r32 - I'd been parroting a stale build number every turn; STOP asserting live state, derive it or omit it. REMAINING design structural: welcome-splash per-strand analysis links; move bucket-tree browser into Analysis; (streak-in-corner, condensed GEN/STR/ACC rail = finer). Absorbs still pending (marking-side): residue engine, error-ID qtype, accent docking. r48 - DESIGN OVERHAUL Phase 2 (structural) + a REAL BUG I owned: the r40 analysis-scaffold wire-up had SILENTLY FAILED (un-asserted .replace missed) - buildAnalysisHeader/renderMisconceptionCanvas/buildKpiStrip were DEFINED but never CALLED, so the switcher/KPI/Misconceptions-tab never rendered (only the coverage matrix + person grids did). Now wired for real (every edit asserted). Analysis surface is now the design's THREE tabs: Grammar (KPI snapshot + coverage matrix + gaps + person×tense/×class) / Vocabulary ('to be developed' placeholder) / Misconceptions (top errors + family heatmap + lens strip + placeholders), with a '← Back to practice' exit. Coverage palette A/B TOGGLE RETIRED - clear is the permanent default (Smith/design). r47 - wine accent nudged #8f3a44->#8f2b45 (less brown). r46 - CEFR now re-scopes the panel (renderLiveStats added to filter onChange). REMAINING design: mobile bottom tab bar; welcome per-strand analysis links; move bucket-tree into Analysis. r45 logo, r44 wine reskin P1. r43 - FIXED: selecting CEFR now re-scopes the coverage panel (Smith flagged it twice). Root cause: the grammar filter bar's onChange re-rendered the DECK but never called renderLiveStats, so the panel only re-scoped on Mark, never on a level toggle - my r37 rootInLevelScope guard was correct but never re-ran. Added renderLiveStats() to onChange. NOTE the scope test is core+preview, so A1 keeps things PREVIEWED at A1 (e.g. passato prossimo); advanced tenses (congiuntivo/passato remoto/pluperfect) drop. If Smith wants a harder cut (core-only), it's a one-line change to rootInLevelScope. r45 - knowledge-atom LOGO added (3x3 wine L-trace in header + favicon.svg). r44 - DESIGN OVERHAUL Phase 1 wine reskin (--accent slate->wine across 94 uses, Fraunces+Lora, lingu·ics wordmark, RWG ramp kept semantic). r43 - DESIGN OVERHAUL Phase 1 (Claude Design wine reskin; Architecture_Housing_design_overhaul_reconciliation says NO hard conflicts, fold the visual over the still-valid engine). Brand accent slate #3a4f8a -> WINE #8f3a44 across all 94 var(--accent) uses (0 raw slate left) + wine ramp tokens --p..--pDeep + CEFR tints re-tinted wine; Fraunces headings/wordmark + Lora body via Google Fonts; wordmark 'lingu·ics' (ics in wine); 'housing/prototype' chrome dropped, Vocab->Vocabulary. RWG mastery ramp kept SEMANTIC (not wine-ified) per spec. NOTE much of the design's STRUCTURE already shipped this session (analysis surface r40-42, clear-palette r39, question-shaped counts r36, no only-touched r37, person bands). REMAINING design phases: analysis as 3 full-width tabs (Grammar/Vocab/Misconceptions) + 'Back to practice'; coverage clear-palette as DEFAULT (retire A/B); mobile bottom tab bar; knowledge-atom logo; welcome per-strand links. ABSORBS (marking-side, separate, additive): residue engine, error-ID qtype, accent DOCKING (0.8 wrong-kind / 0.5 omission - supersedes 0.9-flat). WANTS a push + Smith's eyes (fonts/wine/oklch unrenderable here). r43 - CARD partial (Smith's confirmed subset): (1) the 'Use' cue label now reads 'Register:' for MULTI-WORD cues only, so context/register cues get it while verb cues like '(avere)' stay 'Use'; (2) choose-or-type chips are now STACKED and NUMBERED 1/2 (badge); (3) the accent bar is DROPPED whenever choice chips are present (was hidden only in select mode). DEFERRED (subtle, want a real render): the segmenter colon-gap and an explicit 'Complete with one of these' instruction line. STILL QUEUED: panel-top (vocab strip pinned to top of stats panel + 'Linguics' brand fixed + streak surfaced + level control) - sticky CSS, best verified on a push. r42 - THREE more analysis views live + registry wired. Loader now pulls data/misconceptions.json (17 families + 81 specifics) and data/misconception_lenses.json into LL.misconceptions/LL.lenses. Canvas A: person x CLASS grid live (io..loro x conjugation classes from the event 3rd-segment). Canvas B: FAMILY HEATMAP live (17 families as error-count bars; family = misconception-id prefix, harness-verified) + SURFACE-LENS STRIP live (8 lenses from the lens file, count = sum of member-family hits, verified). Remaining placeholders (6) are genuinely upstream-gated: tense-choice confusion matrix (needs Phase-2 chosen-wrong-tense tagging), piacere grid (needs analyst bespoke spec); cross-kind / region x family / over-under (mirror_of) / strand (v15 comprehension) - all Architecture/analyst data. So the buildable-now analysis views are DONE; natural point to pivot to panel-top + card. r41 - IN-PLACE CELL DETAIL shipped (Smith 'we don't want to leave this page'): clicking a coverage cell now opens an on-page panel (the topic's areas + correctness, misconceptions logged in the topic, opt-in 'Practise these ->' button) instead of jumping into the drill -> task 14 (coverage findability + no-leave) COMPLETE. PERSON x TENSE grid now LIVE on Canvas A: io..loro down x 9 tenses across, cells coloured by correctness from person-tagged verb events, sparse-friendly (empty = unpractised), aggregation harness-verified. Remaining Canvas placeholders (next): person x class, tense-choice confusion matrix, piacere; family heatmap, surface lenses (needs lenses.json load), cross-kind, region x family, over/under, strand. r40 - ANALYSIS SURFACE scaffold shipped (Smith: 'yes, analysis surface'). The coverage view is now a two-canvas Analysis surface: Coverage <-> Misconceptions switcher, each an ordered vertical SCROLL of view-sections (analyst's data/misconception_canvas_views.md). LIVE views - Canvas A: Snapshot KPI strip (answers / fully-right / mean-credit), the coverage matrix (reused, palette toggle intact), Not-yet-attempted gaps; Canvas B: Top recurring errors (counts whatever misconception tagging exists - thin now, fills as it lands, per Smith 'data catches up'). PLACEHOLDER sections (build-order-gated NOT data-gated, turning live next): personxtense, personxclass, tense-choice confusion matrix, piacere; family heatmap, surface lenses (needs lenses.json loaded), cross-kind, regionxfamily, over/under, strand. FINDABILITY (task 14): persistent 'Open analysis ->' button in the stats panel + 'See your analysis ->' on the welcome screen; STILL OPEN - matrix cell click still calls coverageDrill (leaves the page), in-place cell detail is next. Mobile-considered CSS (switcher wraps, sections stack, matrix scrolls-x); general mobile sweep queued (task 16). KPI+top-errors counting harness-verified, node clean. r39 - coverage PALETTE TOGGLE wired (Smith declined a local server: 'wire it up with a toggle, get rid of it later'): default 'clear' = in-scope-unpractised WHITE so partial coverage stops reading as empty + non-existent gaps AND unobtainable cells solid GREY #d9d6cc (same status, Smith's ask); toggles back to 'classic' cream to compare; description reflects the active palette. Verify on push, then drop the toggle & keep the winner. CONFIRMED, NEXT on Smith's continue (BOTH AT ONCE): (a) top-of-right-panel = vocab strip -> pinned top of stats panel + 'Linguics' brand pinned (never floats) + streak surfaced there + level control repositioned; (b) card = 'Use'->'Register:', options stacked & numbered 1/2, instruction line, fix segmenter colon gap, drop accent bar when choice chips present. r38 - coverage sediment now SORTS practised strips by correctness so the darkest green (best-covered) sinks to the very floor, red/low rides just under the unpractised 'to get' band (Smith). PENDING browser-verify (shade-subtle, want it rendered first): partial coverage reads as EMPTY because the rwg ramp is white at ~50% and meets the #faf8ef cream cell bg; sized-box unobtainable TOP (short box in a tall row) shows cream, must be GREY like coverage-na. PENDING Smith-confirm (UX, the important ones): coverage FINDABILITY - only reachable from the entry-screen 'See your coverage' link + stranded after refresh; and cell click calls coverageDrill (LEAVES the page) but should open IN-PLACE detail (misconceptions + attempted/available counts via nodeQuestionStats), relabel drill->in. r37 - removed the dead 'only touched' toggle (index.html + app.js, 0 dangling refs); LEVEL now thins the OVERVIEW TILES too - renderLiveOverview bypassed rootInLevelScope so the tree was scoped but the tiles were not (Smith: 'shows in the stats but they don't reduce'). PENDING Smith-confirm (design): vocab strip -> top of right panel pinned + streak surfaced there + level control repositioned (items cluster at the panel top); question-card redesign (segmenter colon gap, 'Use'->register label, stacked numbered options 1/2, drop accent bar when choice chips present). r36 - tile headline is QUESTION-shaped ("N attempted of M available questions" + "K correct out of N attempted"); the leaf-touched ratio + proficient count are dropped from grammar tiles (vocab/diagnostic tiles keep the leaf line, guarded on nodeQuestionStats.total==0). JOIN KEY shipped: attempt_id now rides every pulse row AND is embedded as an FK inside each misconceptions_json object, so any nested record joins back to the full row instead of copying its columns (answers Smith's "are they not joinable" - they are now). r35 - stats hovers now question-shaped ('N attempted out of M questions' + 'K correct out of N attempted', via a per-bucket item index w/ ancestor prefixes; diagnostic trees get an events fallback); per-row last-N ticks retired and replaced by ONE global 'last 10' report row above the overview (Smith's mid-turn refinement). r34 - extra_json was EMPTY in the live sheet (my v4 claim wrong, owned): estate script sweeps top-level keys only -> payload flattened (answer/expected lead, jsons pre-stringified); BLUE cell deleted (both shades now gone); Smith's contract registered: full fired-against record per row via markpoints_json; Architecture asked re untagged-wrong fallback. r33 - PULSE LIVE (Fletcher's 13 rows in the sheet); Smith's row-review shipped: raw+expected answers lead extra_json, [object Object] misconception-column bug fixed pre-fire (blanks = only 221/4944 guards tagged - Architecture's rollout), log-out (cohort-scoped) + gate name-prefill, tiles say 'N/M tried · K proficient' with 70% threshold stated. r32 - Smith's tidy-up round: strip reworked (5 gapless 1k boxes, 2px matched slivers, Vocabulary caption, ACC/STR-inert/GEN boxes, on translation too, u00b4 CSS-escape bug fixed), COMPULSORY light sign-in gate, sidebar level-scoping (vocab roots exempt), green cell retired, vocab restriction-focus + palette report removed, bigger level toggles; ONE clarify pending (13/23 counts vs person-band emptiness). r31 - grammar PULSE STRIP shipped (Smith-direct prototype): vocab rank mini-map (3 chunky 3x3 boxes = first 5k, 15 slivers to 20k, wash + live green/red fires, click->vocab coverage) + grammar & accent micro-boxes (accent: green on accented-right, amber on slip - the seed of the coming accent viewer). r30 - LOGIN + PULSE BUILT (pulse.js: shared identity key w/ preservation proof, scoped cohort, maximal extra_json at the store funnel, session_start, pill; classes Test/Y12/Y13 interim). Verification = Smith's first row in the linguics tab. r29 - live_round2: operand cues inline (segmenter line-grouping), accent slips = unmissable amber banner + fully-classed events (expected/written/class/chars - feeds pulse payload too); deck-order report does NOT reproduce (all 4 builds shuffle, repro requested); multi_select feasibility answered (cheap, 2 rulings needed). LOGIN+PULSE build planned, blocked ONLY on Smith's interim class list. r28 - Smith's live round on the barcodes: bands flipped to HORIZONTAL STACKS (dot 14x20), footer per-topic inventory retired, quiet 6x5 30-day practice grid + streak label (yesterday-grace rule, no fanfare), build id legible. r27 - paradigm bands GENERALISED (leaf-declared {field,slots,labels}, person fallback, >8 slots = grid, store carries person+slot) ahead of Architecture's migrations; drill-out fixed 3 ways (re-click steps out; out+undo always visible - labelless undo was missing, likely THE defect); drilled box gets stable outline. r26 - person-split Housing half SHIPPED: events person-tagged in store.js (absent stays absent, null carried), finite formation leaves = paradigm barcodes in overview dots + band strips on tree rows, per-band floored mastery (two-for-green per person); coverage matrix left whole-leaf (judged, on record). r25 - drill thread: tree rows were a PAINTED DOOR (cursor+hover, zero handler) now wired; vocab axes already drilled (corrected the record) + tooltips added; index.html EOL-normalised. Person-split migration DONE architecture-side - my half (event tagging + N-band render) is the next round. r24 - live-round UI thread closed same-day: vocab parenthetical inline (flex-column bug family), choose-or-type chips (prompt-stated sets only, 129 items; candidate_forms excluded to honour crit-16 suppression - flagged to Architecture) + Input preference, MCQ number badges. r23 - architect specs landed + acted on: denominator floor SHIPPED (9.5, opt-in per call site - kernel is shared with 4 vocab sites, scope correction on record) + residue survey DELIVERED (1,839 answers, ZERO flips both rules, instrument validated; awaiting go + 2 rulings). Person-split queued behind Architecture's migration. r22 - coverage views TOGGLEABLE per Smith's ruling: equal-boxes (his model, default) vs sized-boxes (r21), cream=unobtained / grey=unobtainable glossary, hatch retired; scoring-formula discussion Smith->Architecture pending. r21 - matrix legibility round: uniform gapless 4px liquid layers (contiguous colour at bottom), scope-scaled row heights, grey unpractised vs hatched NA, family subgroups; A2/B2 counts verified REAL from data. r20 - 'reduced coverage' = matrix rendered off SAMPLE tree during load window (transpose = re-render); coverage now load-gated + refreshed by the loader. r19 - coverage stripes horizontal + sediment-bottom fill + rwg colours (coverageColour had NO red - wrong answers were invisible in the matrix). r18 - coverage matrix cells de-aggregated per Smith (one strip per area, own colours, coverage = colour spread; dense topics band). r17 - Smith's live round: scroll CONFIRMED, pulse moved to the small cell/dot, in-session multi-level toggles (a1b1a2) + composed-scope preservation, accent policy CONFIRMED live (0.9, caffe named). r16 - cue_placement v3 ruling acted on same-day: field renamed wrong_answer_is_form_error_only (claim-named), engine read live with legacy tripwire, migration ball with Architecture. r15 - Smith-direct scroll regression fixed same turn (v7 scrolled the wrong scrollbox + flashed pre-scroll; now ancestor-walk box choice + scroll-then-flash, geometry-mocked harness). Previously: queue cleared at r14 - 8 threads closed/advanced today (scroll fix, entry load race, level picker set, AI-marker accent policy, vocab builder two-lens + ratified 94-chip category picker, cue placement CSS, feedback widget mounted); cue-placement FLAG ruling awaits Architecture
queue:                     # derived from the name-grep 2026-07-17, judged live-vs-historical
  - r134 NAME THE LEARNER'S OWN WORD BACK. Smith was shown `fondo`, guessed "background", was told fondo means depth - and still had to leave the app to find out that background is `sfondo`. "You will always want to know that... I need to know straight away." Now on any miss the engine looks up THEIR word: IT->EN they typed English, so which Italian word is that (background -> sottofondo / retaggio / sfondo); EN->IT they typed Italian, so what does it mean (sfondo -> background). Two words learned per miss instead of one, and the miss stops sending them to a dictionary. EXACT normalised matching only and SILENT when the corpus cannot say it - the r104 phantom-lemmatiser lesson: a fuzzy guess here would invent a word and print it with authority. Candidates ranked commonest-first (unsorted, "background" led with retaggio).   [shipped r134]
  - MY MISREAD, corrected: I heard "I need to know what background is" as the RIGHT ANSWER being unlabelled and shipped r133 for that. That was a real bug and worth having, but it was not what he meant. He meant the reverse lookup above. Both now exist.   [2026-08-05]
  - OPEN, Smith's other half of the same message: he expected the r119 feedback redesign to have rewritten the vocab result panel and says it still looks pre-redesign, "with the background in the house colours". NOT investigated yet - renderResult is shared, so either the vocab path wraps it in an older container or the r119 tokens never reached this card. Worth a look before the next layout pass.   [Next: Housing]
  - DESIGN NOTE from Smith on the same feature: "it's going to work the other way around as well" (done) and "we need space for that to happen" - the result region has to be sized for a reverse-lookup line on top of the diff. r133's .card-result scrollbox absorbs it, but it is worth a look with real misses.   [acceptance, Smith]
  - r133 THE LAYOUT JUMPED, and it was MY r131 fix that caused it. Making the question row `auto` cured the cramped card and bought a worse fault: marking an answer grows the card, which grows the row, which squeezes the frequency square off screen - Smith's two screenshots are exactly that (cold load = prompt above the fold; after a mark = most of the heatmap gone). Row is a STABLE share again, #vocab-host never scrolls as a whole, and the RESULT gets its own scrollbox (.card-result) so prompt/input/buttons never move. The rule this taught: when a region can grow, put the growth INSIDE a scrollbox, never in the grid track.   [shipped r133]
  - r133 THE RIGHT ANSWER WAS UNLABELLED - a real learning bug, and a live consequence of the R1 redesign. R1 deleted the "Right answer:" row because the arrow diff carries it (learner-word -> correct-word). But showLearner is suppressed when the markpoint evidence duplicates the raw response, which is EVERY vocab item (vocab evidence IS the raw answer) - so the arrow went too and the correct word sat there unlabelled. Smith: "have I put background for fondo? I need to know what background is." He was looking straight at the answer and could not tell it from his own. Now labelled "right answer" in exactly the case where the diff cannot speak; the normal two-sided diff is untouched.   [shipped r133]
  - r133 EXPAND now grows UPWARDS per Smith: the breakdown keeps its own strip and steals from the THEMES for extra room, staying in the right column - it never crosses into the question or the frequency square.   [shipped r133]
  - r132 GENDER PANEL EXPAND — Smith's answer to the nine-classes-in-150px problem, and it is better than either of mine: the strip holds COMPRESSED versions with names on hover, and any one of them EXPANDS to take the whole strip region. Neither smaller type nor scrolling; compressed by default, whole-width on demand. Built for gender (the only axis section that exists); expanded it spans both columns and the cells carry their names instead of hover text.   [shipped r132]
  - DESIGN RULED by Smith for the bottom strip + drills: four compressed panels (gender / spelling / accent / stress), hover text, each expandable to own the strip, each with its own internal expansion for all words. AND when you enter a DRILL the rearrangement is massive: that drill's breakdown expands fully and dominates, taking either the frequency quadrant or the whole right half. The drill stays inside the vocab view. The existing drill session panels are KEPT ("quite fun") and gain the axis-style breakdown alongside — they are additive, not replaced. Current .drill-session-panel already spans the whole right column, which is the "takes the themes area" half of that.   [Next: Housing]
  - STRESS FILTERING routed to Architecture, new thread Architecture_Housing_stress_filtering.md v1. Smith asked specifically that Architecture come back to HIM with the question rather than settle it. Housing's factual contribution: every other drill has a filter axis (spelling 8 error classes, gender 7 noun classes, accent placement subtopics, all four share knownDir) and stress has 12,446 items and NO filter of any kind — whole corpus shuffled or nothing — while its analysis side already distinguishes treatment classes in a 4x4 confusion matrix. Four candidate axes laid out as a choice. May be blocked on the phonology-tree question with StressAuthor.   [Next: Architecture]
  - r131 FREED THE TOP, which is what Smith said had to happen before any layout judgement was possible. The page was spending FOUR full-width bands before a single word of content: identity strip, brand row, tab row, filter bar. Two are gone. (1) #identity-box was inserted ABOVE <header> - its own band, AND it scrolled away while the sticky header stayed, which Smith called out as the worst of both; now pinned absolute top-right INSIDE the header, costing zero height and staying put. (2) showStrand() MOVES the active strand's filter bar element into #header-filter-slot beside the tabs - moved, not duplicated, so every listener and re-render inside it keeps working. Applies to all three strands.   [shipped r131]
  - r131 QUESTION ROW IS NOW `auto`, not a fr share. Smith: "you cannot have that bit not showing all of itself in the full with all its glory" - it was getting a scrollbar. The card takes the height it needs and the frequency square takes what is left, on his own ruling that the heatmap can shrink ("doesn't really affect it very much"). Body height calc(100vh - 232px) -> 168px with the two bands back.   [shipped r131]
  - r131 GENDER PANEL MISSING IN MIXED - real bug Smith spotted. vocabHeatmapDir() returns "any" for mix, and the gate tested activeDir === "active", so a mixed session hid the panel even though half its cards are EN->IT production and gender is practised on every one. Now gated on !== "passive": shows for en_it AND mix, hidden only on pure IT->EN.   [shipped r131]
  - r131 strapline now "Language learning, atomised. (Currently just Italian.)" - the parenthetical was asked for two rounds ago and I missed it.   [shipped r131]
  - STILL UNBUILT and Smith knows: the scrub rail, the unified filter object + scope chips, the spelling/accent/stress mini-panels beside gender. Gender is the ONLY axis section that exists today; the other three live solely as the drill session panel. Also unresolved by design: how the gender strip fits 9 named classes in 150px - Smith saw it clipping and suggested smaller words; my read is the strip needs its own horizontal scroll or two rows, and it is the one place his "no scrolling" rule and the content genuinely conflict.   [Next: Housing]
  - r130 QUADRANT FRAME SHIPPED with the real renderers, nothing redesigned. question top-left / axes header / REAL frequency square bottom-left / REAL themes column right, scrolling / gender strip bottom-right. THE KEY MOVE: #vocab-axes-host is now `display: contents`. All five lookups query DESCENDANTS of that host, so keeping the element in the tree keeps every one of them working untouched while its three children become grid items in different quadrants. Moving the sections out instead would have left flashAxisCellsFor and recolourAxisCellsFor finding nothing - no crash, just feedback that stops arriving. Two selftest cases pin both halves (every claimed grid-area is declared; the host is still display:contents).   [shipped r130]
  - r130 revealInAxisPanel GENERALISED: it assumed the scrollbox was #vocab-axes-host, which under display:contents has no box at all. Now walks up from the cell to the nearest ancestor that actually scrolls, stopping at #vocab-body. Still never scrollIntoView, still never window.   [shipped r130]
  - r130 header given air top and bottom (Smith: "a bit too squashed at the top"). CORRECTION on the record: I wrote that the brand should stop dominating; Smith did not say that and says it KEEPS its prominence ("it has never been by much"). Only the spacing moved - brand size, icon and strand nav all untouched.   [shipped r130]
  - NEXT SLICE, not yet built: the ONE unified filter object (ruling D) + the scope chip bar + boxes round the in-scope theme and frequency block; restoring the frequency filter controls INTO the new layout (topN / sub-band / direction / drill levels all still live in #vocab-filter-bar, none lost); spelling/accent/stress mini-panels alongside gender in the strip (only gender has an axes section today - the other three exist solely as the drill session panel).   [Next: Housing]
  - GUESSES AWAITING SMITH'S EYE: strip 158px; columns 1fr/1fr; rows 1fr/auto/1.15fr/158px; body height calc(100vh - 232px). All arbitrary. He said proportions are undecidable until it is real - it is real now.   [acceptance, Smith]
  - MOCKUP PHASE CLOSED 2026-08-05. Smith reviewed v2 and ruled: BUILD IT with the REAL components and iterate live - "I think you just have to make it and see what happens". His faults with the mockup are all the same fault: I DREW the frequency grid and the themes column from scratch instead of showing the real ones. He does not want either redesigned ("I still want that square"; "I like how the themes work"). The scrub rail is exactly right and is the one thing the mockup got right by being live.
  - BUILD SPEC as ruled: quadrant layout, real renderers untouched. Question top-left; the REAL frequency square bottom-left with ALL its existing filter controls restored (topN / sub-band / direction / drill levels were simply missing from my sketch); real themes column down the right, scrolling, NOT redesigned; gender/spelling/accent/stress in the reserved bottom-right strip. Header: Linguics must NOT dominate, keep the icon + strand nav, currently too squashed. THE PAGE MUST NEVER SCROLL - only boxes scroll inside themselves.
  - FIRST SLICE, and the reason this is not a CSS job: #vocab-axes-host is queried by FIVE call sites (renderVocabAxes 4917, recolourAxisCellsFor 4794, flashAxisCellsFor 4876, revealInAxisPanel 4858, the drill session panel 3255). Splitting the panel into quadrant slots means repointing all five at a wrapper, or the flash/recolour lookups silently stop finding their cells - a dead-feature failure, not a crash. renderVocabAxes already appends exactly three sections (freqAxis 5085, genderAxis 5125, themesAxis 5165), so the split itself is mechanical once the lookups are safe.
  - OPEN, answerable from data not opinion: whether the bottom strip holds all the classes WITH NAMES. Gender has 7 drill classes (and 6 in the axes panel), spelling 8, accent 6 placement subtopics, stress its own set. At 150px across four columns, named rows do not fit - so either the strip goes ~200px or the cells stay unnamed with names on hover/drill-through. Smith called the weight right, so I will try 150px unnamed + hover first and show him.
  - PARKED at Smith's request: the entry screen's top row SHIFTS on load (grammar/translation/vocab render first, then two more chips push them down and you can click the wrong thing). Fix is reserved space. He said other things are in the pipeline for that row - wait.   [Next: Housing, after Smith's pipeline]
  - r129 START BUTTON, the unresponsive one (Smith, home page). Two silent failure modes, both fixed: (1) Start was LIVE during the content-load window - until the real banks arrive the app runs on samples, so Start either began a session over sample data or ran a deck builder against a bank that was not there, and the button looked completely normal throughout; it now reads "Loading..." disabled, and the entry screen already re-renders when the load settles on BOTH the success and fallback paths. (2) ANY throw in the start sequence (composeGrammarFilter, buildSpelling/Accent/StressDeck, showStrand) died inside the click handler with no banner and nothing moving - which is exactly what "unresponsive" looks like. Now caught and shown as a named message + a session_start_failed GA event, so the next report is diagnosable instead of a shrug.   [shipped r129]
  - r129 STRAPLINE "Italian, atomised." -> "Language learning, atomised." (live index.html only; the feedback_redesign mockup copies keep the old line as period artefacts).   [shipped r129]
  - MOCKUP v2 CORRECTED: I had misread ruling B - the RAIL STAYS. It is a scrub, not a range selector: one handle sliding the length of the corpus, and whichever thousand it passes over becomes the principal focus. Now LIVE in the mockup (drag it; blocks re-rank as you go) because the feel of the scrub is the one part a static picture cannot answer. Clicking a block does the same thing, so two routes to one act.   [Next: Smith]
  - RULING D (2026-08-05): entry-screen selections are EDITABLE on the page and behave exactly like on-page filters - choose Colours on entry, Colours gets a box in the themes column and a chip in the bar. Smith is right and my "two editors" caveat was aimed at the wrong thing: the risk is not two SCREENS, it is two STATE SHAPES (entry writes rankRanges/themes, the page reads rankRange/topN/subBand). THE BUILD IS COLLAPSING THOSE INTO ONE FILTER OBJECT that both screens write and every panel reads; then editable-here costs nothing, the box and chip fall out free, and the drift class cannot recur because there is no second representation.   [Next: Housing, on Smith's sizing calls]
  - r128 REMEMBERED DIRECTION (Smith): vocab direction was hardcoded "it_en" on every load and translation's way reset to Both on every entry-screen visit, so a learner who always works one way re-picked it every session. Both now persist per browser (localStorage, same grain as input-mode and drill-keep). KEY detail: "" is a REAL choice for translation (Both), so the stored value uses a "both" sentinel to distinguish "chose Both" from "never chose".   [shipped r128]
  - r128 NUMERALS ACCEPTED AS DIGITS (Smith: "accept 10 when the q is dieci"): a closed table, both directions (EN and IT number words 0-20, tens, hundred/thousand/million), full credit, and when the learner typed digits the result names the word back so they still meet the spelling. Deliberately a TABLE not a parser - a parser also accepts "10" for "tenth". Both sides must be numerals, so "sei" glossed "you are" is untouched while "sei" glossed "six" accepts 6. 23/23 cases; 101 vocab entries have a numeral lemma.   [shipped r128]
  - MOCKUP v2 delivered (mockups/vocab_redesign/vocab_redesign_v2.html) to Smith's A/B/C rulings: A click still moves the view; B range varies as now and the "~2k-3k" label moved ONTO the block (no rail - which also disposes of the 990-vs-1000 mismatch, "~" makes it honest); C drill chips stay top-level AND repeat next to their breakdowns, and the analysis panels are NOT tabs. Layout = second-row furniture folded into the top band to buy height; question top-left, frequency bottom-left, themes down the right (scrolling), gender/spelling/accent/stress in a permanent reserved strip bottom-right the themes scroll past. Zings fire on a loop in the mockup to show the whole point: one mark is evidence about five things at once.   [Next: Smith]
  - OPEN: ruling D (entry-screen selection editable here, or clearable-only?) went unanswered; I lean clearable-only, two editors of one state is how this drifted. Plus two sizing calls only Smith's eye settles: reserved-strip height (150px in the mockup) and the left/right split (50/50 vs 45/55).   [decision, Smith]
  - r127 THE VOCAB SLOWNESS IS QUADRATIC, not a render cost. eventsForLemmas scanned EVERY attempt and parsed EVERY bucket id, and it is called ONCE PER LEMMA; the frequency heatmap asks about ~18,800 lemmas per render (990 focused dots + 380 flanking cells holding up to ~99 lemmas each). So one chart interaction re-scanned the learner's whole history ~18,800 times, and the cost is (corpus x history) - it gets slower the more you practise, which is why Smith feels it and a new user does not. Now a lemma->events index built once per render, same invalidation contract as _practisedCache. MEASURED in node (not the browser): a render's asks at 200 attempts 12.2s -> 20ms; at 800 47s -> 40ms; at 2,000 120s -> 90ms. Equivalence 2,400 checks across 6 option shapes, 0 mismatches, ties preserved via a seq tie-break.   [shipped r127]
  - r127 GENDER DRILL two tiers (Smith): seven equal buttons framed a two-way decision as a seven-way one ("it looks just intimidating"). Masculine/Feminine now lead at 25px full width; the five split patterns are a quiet cluster under "or one of the split patterns". buttons[] stays in CLASS ORDER whatever the DOM order because the reveal indexes it by class number. ALSO class 3 relabelled "M or F, depending on the gender of the person" - "depending on gender" never said gender of WHAT.   [shipped r127]
  - VOCAB REDESIGN MOCKUP delivered for Smith, mockups/vocab_redesign/vocab_redesign.html - scope bar with provenance chips, view/filter separation via a drag rail + a named "Practise these" button, permanent "the 18,000 most common words in Italian" subtitle, scope outlined in BOTH panels, right column = scrolling categories with theme/gender/spelling/accent/stress tabs, visible route back to the entry screen. FOUR rulings asked (A click-to-move-view, B range vs whole thousand, C drills as chips or tabs, D entry selection editable here or only clearable). NOT BUILT until ruled.   [Next: Smith]
  - ROOT CAUSE of "the entry-screen filter does not show on the vocab page": the entry builder writes vocabFilter.rankRanges + .themes; the vocab filter bar renders topN + subBand + a bare "N of 18,048" count. DIFFERENT FIELDS - so an entry-screen choice is structurally invisible, not merely unlabelled, and themes are never rendered anywhere. Also the block-click handler sets rankRange AND refocuses in one gesture, which is the view-vs-filter conflation Smith hit trying to move to 2,000-3,000.   [derived 2026-08-05]
  - NOTED: the route back to the entry screen EXISTS (brand-mark click -> LL.showEntry()) - a discoverability fault, not a missing feature. Smith refreshed instead.   [derived 2026-08-05]
  - OPEN, needs Smith: the unresponsive-button report has no repro (entry screen, "they start and it was unresponsive"). Candidate is the content load window - LL.contentLoading gates the entry screen. Need: which button, and whether it was within a few seconds of a cold load.   [acceptance, Smith]
  - FLAGGED not fixed: the focused grid covers 990 ranks (11x10 boxes x 9 dots) while ticks and block ids read in 1,000s, so "ranks 2001-2990" is a 990-wide block wearing a 1,000-wide label. Cosmetic today, load-bearing if the redesign makes the range draggable.   [derived 2026-08-05, Next: Housing after ruling B]
  - r126 THE VOCAB "JUMPING" WAS NEVER A SCROLL BUG - it was an UNBOUNDED PANEL, and Smith's own diagnosis ("the right-hand panel needs to be independent") was right where r98's was wrong. #live-stats has carried max-height + overflow-y:auto + position:sticky for months, with a comment saying it exists so reading the panel never drags the card off-screen; #vocab-axes-host had NONE of it - flex 0 1 65%, normal flow, unbounded, ~990 freq dots + flanking blocks + gender/theme axes. So the vocab page is several screens tall with a short card at the top, and ANY nudge loses the question. r98 (vocab guard) and r99 (five preventScroll sites) were true fixes to real faults and both symptom-level: on a one-screen page there is nowhere to fall. Panel now gets the #live-stats contract exactly, released in the narrow breakpoint where it stacks.   [shipped r126]
  - r126 ELEVEN unqualified focus() calls swept out of app.js - r99 found five, these are the other eleven, and the two that matter fire ON THE ANSWER: the residue banner and the second-chance banner both append into the result host and then focus a button inside it, which is a guaranteed downward scroll. Also the accent-bar key, the slash menu return-to-input, the sign-in box. ZERO remain; a selftest case holds the line, and that is the invariant that would have prevented eleven regressions. feedback.js has 2 more but it is shared WEB_KIT code with a fixed-position panel - noted, not touched.   [shipped r126]
  - r126 revealInAxisPanel: the flash feedback had to survive the panel becoming a scrollbox, so a flashed cell is brought into the PANEL'S OWN viewport by writing panel.scrollTop. Explicitly NOT scrollIntoView (walks every scrollable ancestor incl. the window - the mechanism r126 removes). This is the safe half of the feature r98 had to ban outright.   [shipped r126]
  - SCOPE: renderGenderDrillCard / renderSpellingDrillCard / renderAccentDrillCard / renderStressDrillCard ALL render into #vocab-host beside this panel, so Smith's older gender-drill "scrolls down a bit after each answer" is the same structural cause - five card types fixed by one rule.   [derived 2026-08-05]
  - NOT VERIFIED BY ME: I cannot run a browser, so the jump is not REPRODUCED - the cause and the eleven nudges were found by reading. Smith's live look is the verification and the question is narrow: does the card stay put on vocab and gender through a run of right AND wrong answers?   [acceptance, Smith]
  - r125 MULTI_SELECT CHROME shipped, cue_notation_renderer Housing side DISCHARGED at v6. The instruction now has its own accent-coloured row between prompt and choices; it previously rode inside the faint CEFR breadcrumb, in the same grey as the item id - metadata furniture, not an instruction, which is almost certainly WHY twelve authors wrote the sentence into their prompts. Chrome states the scoring rule too ("a wrong tick cancels a right one"): credit is (correct - wrong)/total floored at 0, and an engine that silently punishes a guess the widget appears to invite is not being honest. Flagged for pushback rather than assumed.   [shipped r125]
  - r125 PROMPTS DROP THE SENTENCE AT RENDER TIME, not when the data catches up: LL.stripSelectAllChrome in engine_axes.js (so selftest reaches it without a DOM). No window where the learner sees it twice, and the authors' edit becomes optional tidy rather than a coordination step. SURGICAL because it must be - op_ord_climb_ms_01 writes "[fem 'it' = la lettera; select all that apply]" and a blunt strip deletes the cue and leaves the item unanswerable. Swept every multi_select item: 12 carried it, 12 cured, 0 left in, 0 clean prompts altered. 3 selftest cases incl. cue-survival.   [shipped r125]
  - COUNT CORRECTION derived from disk: the routing thread said 11 items (pronoun 6 + pronominal 5); it is 12 (pronoun 7 + pronominal 5) - op_dop_vi_you_03 was uncounted and is the awkward one (instruction trails a Source: line). Every multi_select item in the estate carries the phrase.   [derived 2026-08-05]
  - FOUND, NOT BUILT, routed to Architecture: multi_select BYPASSES the shared prompt renderer (prompt.textContent = q.prompt), so all 12 items render their bracketed cue raw while identical notation renders properly one qtype over - same class as the spelling deck never serving error_id. Did NOT wire renderPromptElement because it mounts the vocab/retrieval help affordances and this card's doMark never consumes vocabHelpsUsed: a Help button whose misses go unrecorded is worse than a raw cue. And it is not hypothetical - op_ord_ne_pp_ms_01 already carries a vocab_help entry (mele -> "apple - mela (f)") that has NEVER been shown to a learner. One decision: does multi_select join the shared renderer, and does doMark gain appendVocabHelpEvents? It changes what is RECORDED, so it is not mine to take.   [inter_chat cue_notation_renderer v6, Next: Architecture]
  - r124 RESIDUE ENGINE WAS WIPING THE ACCENT-FOLD RESCUE - an estate-wide false MISS, ruled by Architecture (InterrogativesAuthor thread v2) and unbuilt when I took the seat. The residue block re-subtracted any_phrases STRICTLY while the markpoint may have been credited through the FOLDED rescue, so on "piu alto di" the accented phrase was not found, its words survived as residue, and the docked partial was zeroed. Measured old-vs-new over every single-markpoint free-text item: 178 accent-off probes went miss 174 -> partial 174, miss 0. Built v1's remedy (fold the subtraction) not v2's literal one (skip the zeroing), because a blanket skip contradicts v2's own second clause - junk in an accent-off answer would escape entirely. Regression: 2,052/2,052 correct answers still full, 2,052/2,052 junk answers still zeroed.   [shipped r124, thread CLOSED v3]
  - r124 RESIDUE PUNCTUATION, same block: norm() only strips ?.,;:! before a space or end, so a "(amico)" cue or a "...?'" sentence close left prompt words punctuated and they never subtracted. A learner writing the whole sentence was zeroed. False residue on blank-filling items 1,398 -> 305, and the surviving 305 are correct (they echo the English [gloss], which is deliberately unlicensed).   [shipped r124]
  - r124 R8 white-space: pre-line applied to BOTH .result-explanation (innerHTML path - HTML collapses newlines exactly as textContent does) and .gd-declension (the drill path v4 named). Applying it only where the thread pointed would have left the drills unable to paragraph, which is where most of the 238 explanations are read. Inert until the re-paragraph pilot is blessed.   [shipped r124, riders v5]
  - r124 ACCENT a-b-c RE-CONFIRMED against the regen that LANDED 2026-08-04 00:11, i.e. after the r123 build - r121's lesson applied deliberately rather than trusting the v12 stamp. 306 items x every choice, 637 cells: ZERO defaulted outcome classes; the new `false_alarm` class rides the flat judgement path as ruled (absent from ACCENT_OUTCOME_LEAF, so tag-first declines it by construction); docks 306@1.0 / 25@0.8 / 47@0.5 / 62@0.5 / 39@0.4 - the 0.4 severe-omission split proves accentPlacementClass is reading the new full-id era; all 9 buckets fired land in the tree.   [verified r124, seed_frames v14]
  - TAKEOVER CORRECTION on the record: the outgoing seat wrote "queue EMPTY by the board". It was not. Two commissioned builds (residue fix, R8) were routed to Housing AFTER that sweep or under another chat's filename, and both were invisible to a *Housing*-filename triage. The header of the residue thread still read "Next: Architecture" and "Current version: v1" while v2 routed the code to me - the same stale-header class the handoff warns about, now hit twice more. Sweep the LAST version section, never the header.   [derived 2026-08-05]
  - OPEN, small, nobody blocking: multi_select CHROME. "Select all that apply" is qtype furniture living in 11 prompt texts (pronoun x6, pronominal_verbs x5); authors are holding their prompts until the renderer says it natively (cue_notation_renderer v4/v5). buildMultiSelectResult is already live - this is display only.   [inter_chat cue_notation_renderer, Next: Housing]
  - NOTED not owned: 18 data/grammar_questions_*.json sit MODIFIED-UNCOMMITTED from Architecture's 2026-08-04 21:12 migration, so the live site is serving the committed versions. content_check is green on both. Architecture's to commit, flagged so it is not mistaken for Housing drift.   [derived 2026-08-05]
  - r123 SMITH-DIRECT (live review round 1): result line now names its two currencies - "Elements ticked: 8 of 9 (89%)" + "Impressionistic: 100%" (the marker's holistic 0-1, hidden on stubs) - and the session chip runs BOTH independently ("Session · elements 34/45 (76%) · impressionistic 95%"; stub attempts no longer add a phantom possible-of-1). Bench model picker carries derived per-mark costs (~6k-in and full-menu variants); the "32p per question" figure was the $0.32/M-INPUT price wearing per-question units - real qwen3.7-plus cost is ~$0.003/mark current mode, ~$0.017 full menu, matching Smith's observed ~1p.   [shipped r123]
  - r122 HARDENING: tonight's rulings PINNED into selftest (now 40 tests / 9 groups): 6 docking/double-fire cases (0.4 on BOTH subtopic eras - the regen-broke-r118 regression is now a standing net; inserted 0.5; the zero-weight suppressed tally row; both judgement variants) + 4 predicate cases (isLearnerFacingNode retired/placement/ordinary; isRegistryMisconceptionId pass/refuse with stubbed registry). Reveal-bar comment un-staled (gate itself widened r119). No census tool exists in tools/ to align - the predicate is the alignment.   [shipped r122]
  - r121 STR/ACC CHIPS + A7 + DOUBLE FIRE shipped: ACC placement×outcome (double-fire events; green/terracotta ramps never share a scale; pre-rewire answers captioned not mislabelled), STR treatment-by-class (+pointer to the 4x4; mechanism/tags toggles NAMED as StressAuthor-blocked), hearing-vs-writing cross on the Rev-27 final-stress class, A7 piacere verb×dimension grid (verb_identity where set, dispiacere via subtopic, else piacere; bastare empty row; joined-count caption). Double event per seed_frames GO: placement fires EVERY attempt, outcome leaf additionally on errors as a zero-weight suppressed synthetic markpoint (no marks/docking effect). Agg harness 8/8 + engine harness 10/10.   [shipped r121]
  - REGEN BROKE r118 UNDER ME, caught same night: the Defect-1 rewire moved subtopic to FULL placement ids while r118's 0.4 severe-omission dock + placement axis keyed on the seed era's accent.<class> - meaning_pair/tense_bearing omissions were docking 0.5 and the axis read whole ids. accentPlacementClass now normalises BOTH eras. Lesson re-learned: the repo advances under you; re-derive data shape at build time, not from your own morning.   [shipped r121, seed_frames v12]
  - r120 MISCONCEPTION FIELD PURITY shipped, thread CLOSED at v2: emit gate at the buildMcqResult funnel (canonical registry ids only; proposed flag keeps its sanctioned channel; stress_attempt + accent axes keep the descriptors so the matrices lose nothing) + read-time defence accessor on all 3 drill-down consumers (~30k historical phantoms inert; 29,964 stress.confusion.* re-derived vs Architecture's ~30,500; registry = 106 specifics, Set cached on the registry object so reloads invalidate). Harness 8/8.   [shipped r120]
  - STR/ACC CHIP SCOPING derived from disk: stress_mechanism/stress_tags exist NOWHERE in data (grep 0) - the accent_stress thread's 'membership AND treatment toggleable' axes await the StressAuthor pipeline (data_spec thread opened tonight). Buildable now: ACC report from r107 axes, STR from stress_attempt (4x4 already live), perception-vs-production cross on final-accented words. Grammar_pulse_strip v5's 13/23 ask verified discharged (header Next post-dates v5; r35/r52-era relabel).   [derived 2026-08-03 23:1x]
  - r119 FEEDBACK PANEL shipped per riders R1-R6 (handoff steps 1-8): tokens retuned + --touched + PROVISIONAL --green-partial #5d9a77 (Design owns the value); R1 deletions (per-word colouring gone; You-wrote/Right-answer rows -> arrow diffs; renderer is SHARED so grammar cards too, flagged for pushback); R2 One/Two-ways from reference_translations (data-side, no worker change); R3 last-10 gains the strictly-between \u2248; R4 partial family + amber's ONE job (mixed span) + 3px got-it vs 2px partial/wrong; R5 LL.isLearnerFacingNode wired into matrix scope + live tree + grey copy 'nothing to practise here yet'; R6 honest stub early-return; step-7 span verdicts LIVE (DOM-stub harness 6/6 incl. overlap->mixed and credited-head-stays-right) with the missing-word tick DEFERRED (needs a worker 'after' position hint - proposed R7-adjacent). Reveal gate widened to retrieval_help_lemma (instrument-A clickable cue).   [shipped r119]
  - Riders thread had NO header block (Architecture shipped it loose); one added at v2 so the board and Next-sweeps read it correctly.   [hygiene]
  - r118 ACCENT RULINGS shipped (design_overhaul v4 + seed_frames v7 a-b-c): inserted 0.8->0.5 (live drift closed), omitted docks 0.4 on meaning_pair/tense_bearing subtopics, judgement misses classify TAG-FIRST and retarget (the ruled emission) behind a new isJudgement guard pinning credit at 0 - untagged judge items (all 158 today) byte-identical until the regen tags land. Harness 8/8. Double event (placement+outcome) deliberately deferred to regen acceptance: today's subtopics don't resolve, the events would land nowhere.   [shipped r118]
  - QUEUE CORRECTION on the record: my takeover sweep claimed "queue empty" - WRONG. It swept *Housing*/*ALLAUTHORS* filenames plus a grep that missed bold **Next:** lines, and I never ran status_board.py, the ratified queue instrument. The board surfaced the SpellingAuthor stale header (now fixed; error_id was discharged r110) and the feedback_redesign_riders commission. Board FIRST before any queue claim from now on.
  - NEXT UP: feedback-panel redesign per riders (handoff.md steps 1-4 mechanical now, 5-8 per R1-R6: R1 kills "You wrote/Right answer" + per-word green/pink; R2 One-way/Two-ways from authored reference_translations, no worker change; R3 partial = graded correctness never weight; R4 --green-partial everywhere the handoff said amber, amber's ONE job = mixed span underline; R5 grey = active leaf with zero live items, "Nothing to practise here yet"; R6 honest stub). STR/ACC chip surface rides the round.   [in progress]
  - INSTRUMENT-A RENDER NOTE derived: the r90 reveal affordance is gated on provenance==="lemma_retrieval_pilot" (app.js ~1930/5808), so instrument-A items need the gate widened (fire on retrieval_help_lemma presence) before the clickable cue works outside the pilot. Small; rides the panel round or the dispatch, whichever lands first.   [queued]
  - r117 MEMBERSHIP GATE shipped, candidate_forms thread CLOSED at v2: credit_only silence now yields a REAL formation miss when the answer is in the item's candidate_forms but is not the target (exact norm() equality, formation-side markpoints only; result carries in_candidate_set + an evidence line). Measured the overlap FIRST: 169 candidate_forms items, 12 pilot items, intersection 0 -> no live behaviour change, no provenance flag (the gate is inert until an item opts in with both fields). 5 new selftest cases (group "candidate_forms membership") + node harness 9/9 incl. pilot regression. Unblocks Architecture's instrument-A dispatch (adj 30 / noun 7 / imperative 6 / demonstrative 7).   [shipped r117]
  - markpoint_level_match_at v3 lodged: v2's closing "no change needed" was FALSE for the rossa row (no candidate_forms code existed; grep evidence) - corrected on the record before it could kill the build; v2's 08-02 date on an 08-03 section flagged.   [inter_chat v3]
  - bespoke_grids v3 DISCHARGE STAMP: A6 (shipped r110) was still reading as open work. A7 verb_identity still absent from the data; B6 awaits mirror_of.   [inter_chat v3, Next: MisconceptionAnalyst / Architecture+authors]
  - TOOLING r117: preflight.py now mkdtemps its scratch dir (root-owned /tmp leftovers from a prior session collided with the hardcoded names and killed the run BETWEEN the js checks and the worker check, so the worker check was silently not running sandbox-side); content_check's no-tree headline now counts DISTINCT missing ids (32) identically in plain and --verbose (was: number of report LINES, 2 plain vs 13 verbose).   [shipped r117]
  - >>> A FRESH SEAT SHOULD READ _status/HOUSING_HANDOFF.md BEFORE THIS QUEUE. It carries the three pre-push checks (preflight / content_check / selftest), the engine facts that cost real bugs to learn, the triage errors to avoid (sweep on the Next: LINE not the filename; a Next: Housing header may be finished), and who owes what.
  - ANCHORING SWEPT WHOLE-BANK after the r116 find (which was luck, not method): 3,158 free-text phrases, 61 unanchored (2%), 12 nesting inside a real Italian word - and ALL TWELVE are deliberate, the lemma-retrieval pilot vocabulary markpoints where a bare stem ("parl") is the point and the formation markpoint beside it IS anchored. Estate anchoring discipline is sound; the r116 fault was one author writing the anchor in a form the engine discarded, not sloppy authoring. Now a standing invariant in content_check.py with the pilot exempted by provenance.
  - r116 MARKPOINT-LEVEL match_at WAS SILENTLY IGNORED - live false credit on 32 passato-prossimo items (64 markpoints). Anchoring is per-phrase by design and 2,569 markpoints use it correctly, but an author can also write match_at at MARKPOINT level and the engine discarded it without a word. Consequence: "ho parlatoo" (misspelt participle) and "hoparlato" (no space) BOTH scored full marks, because the correct string sits inside the wrong one and nothing enforced the boundary. Fixed engine-side: findMatchingPhrase takes the markpoint match_at as a DEFAULT, per-phrase still wins, both accent-folded fallback paths inherit it. Chose engine over a 64-key data migration because it also fixes every future author who writes the natural-looking form. Regression: all 32 items still score full marks on their own correct answer.   [shipped r116, inter_chat Housing_Architecture_markpoint_level_match_at.md]
  - r116 SELF-TEST now pins the six GRAMMAR MARKER SEMANTICS that every false-credit bug in this estate has turned on: positive any_phrases BEATS must_not_include (else-if gating); markpoint-level match_at honoured; per-phrase still wins; residue fires on single-markpoint items only; residue ignores prompt-supplied words; norm() folds apostrophe and hyphen. All six were documented across half a dozen threads and enforced by nothing.
  - CORRECTION to my own status: the residue engine is LIVE and correctly gated (v3 gave the go), not "awaiting Architecture". It is what stops `sta` being credited inside `stava` on a single-markpoint item.
  - r115 JUDGEMENT CLASS - the self-test caught a bug in my own r107 work on its FIRST RUN. 158 of 301 accent distractors are judge-the-form items ("Correct"/"Incorrect"), so accentDiff correctly returns null and my fallback returned wrong_kind for ALL of them: 158 events on .wrong_mark, a leaf none belongs to, and 0.8 credit for a two-way guess once docking landed. Exactly the default-dressed-as-derivation fault r107 existed to fix, surviving in the half of the data I never sampled. Now `judgement` is its own class: keeps the DECLARED bucket (no retarget), docks nothing (0 when wrong). After: 0 distractors classified by default. ALSO: my first version of the test asserted the DATA (every distractor has a choice_tags class) and failed 158 times uselessly; the invariant that matters is that every class is DERIVED, never defaulted.   [shipped r115]
  - r114 ENGINE_AXES EXTRACTED: inferChosenTense + the accent axes/docking were pure logic sitting inside the 480k-char DOM file, reachable only by booting the whole UI. Now housing/js/engine_axes.js, loaded before app.js, callable as LL.* from the app, the bench and the self-test. Logic that decides what gets RECORDED should not need the UI to run.
  - r114 housing/selftest.html BUILT: every case names the SILENT fault it protects against (the dead 3-apostrophe escape, the phantom lemmatiser, the no-oping scroll, the unserved spelling items, temperature 1.0). Runs against the real modules and real content; must be served, not opened off disk. A test that cannot say what regression it catches does not belong in it.
  - BUCKET LAYER AUDIT (Smith: "have you checked your buckets?" - I had only tripped over the phonology gap, not looked). 34 trees / 596 nodes. CLEAN: 0 duplicate ids; all 32 bucket_suggestions files fully minted. FOUND: (a) `verb_form` has 11 children and DOES NOT EXIST - eleven trees claim a parent no tree defines, so anything walking upward stops a level early; mint it or null the parent_ids. (b) 23 of 440 leaves are targeted by NO item and NONE carries the `stub` flag, so the renderer counts them as achievable - a permanent ~5% drag on every verb coverage figure; they are the per-family tense-discrimination leaves whose content went to tense_choice instead. Both Architecture to rule.   [inter_chat phonology thread v2]
  - r113 COST CAP was silently killing most bench runs (Smith: "most test calls are exceeding the call cap and so not run"). COST_CAP_PER_CALL_USD=0.03 was set when every call was DeepSeek at ~0.1c; the worker estimates with MAX_OUTPUT_TOKENS as worst-case output, so on the FULL menu Sonnet 5 estimates $0.132, Grok 4.3 $0.075, GPT-5.6 Luna $0.068, Haiku $0.066 - all 413 before the model is ever called. Fix: max_cost_usd accepted per call, CLAMPED to HARD_COST_CEILING_USD=0.25 so no client can authorise unlimited spend; learner default unchanged at 0.03. The 413 now names the model, the token estimate and the ceiling instead of just "exceeds the cap". Bench gets a cost-ceiling box AND predicts the refusal up front from a mirrored price table - a blocked mode/model now reads red BEFORE you spend the round trip.   [shipped r113; worker half needs the deploy]
  - tools/content_check.py BUILT 2026-08-02 - turns the hand verification into a repeatable content-invariants check (unregistered bucket ids grouped BY ROOT, it_en production-only floors, duplicate external_ids, missing direction fields, out-of-range answer indices, vocab entries with no gloss). Nothing it reports is a code failure; every line is content the engine cannot act on.
  - FINDING 3 (found by content_check on its FIRST run): THERE IS NO `phonology` BUCKET TREE. All 12,446 stress items fire phonology.stress.* across 31 distinct ids, and no manifest topic loads that root - so the whole stress strand is invisible to coverage, tiles and the tree browser. It looks healthy only because the stress drill has its own bespoke 4x4 matrix reading stress_attempt directly. Same shape as the accent leaves before r107. Housing does not mint buckets, and there is a prior question (should stress be in coverage at all?) that Architecture must answer first.   [inter_chat Housing_Architecture_phonology_tree_missing.md, Next: Architecture]
  - DISCHARGED on AccentAuthor_Architecture_seed_frames: both Housing items done - class->credit map r112, apostrophe-input fix r101.
  - VERIFICATION PASS over r90-r112 (2026-08-02). Clean: 48/48 loader assets 200 + parse; classifyCue over 1,603 cued prompts 0 throws; buildBucketContext over 875 items 0 throws and 0 phantom vocab lemmas; normaliseAccentInput swept over 44,381 authored strings with only the intended escalation, and the rewriter is correctly gated OFF wherever the learner types English (target_lang present on all 875: 618 it / 257 en).
  - FINDING 1 (BIG, not mine, needs a RULING): 190 of 257 it_en translation items - 74% - have EVERY authored required_bucket removed by the direction filter, leaving the marker no mandatory bucket at all. 286 floor buckets lost in total; most-lost are passato_prossimo.auxiliary.avere_default (15), participle_form.regular.are_ato (9). Either the it_en floor is decorative (then the authoring gate should refuse a production-only floor), or 190 items want re-authoring, or the filter should pass a recognition variant. Bears on the crosstopic thread: part of "no breadth on it_en" was the marker having no floor to start from.   [inter_chat crosstopic v21, Next: Architecture]
  - FINDING 2: trans_imp_en_it_st_cc_01 requires verb_form.congiuntivo.trapassato.counterfactual_protasis, which is registered in NO bucket tree. Silent both ways - buildBucketContext skips it, the worker fires it anyway from the item JSON, and the event lands on an id nothing aggregates. Housing cannot mint buckets.   [Next: Architecture]
  - translation_stub_honest CLOSED 2026-08-02 v2: both faults were fixed at r83 (markpoints:[] and marks_possible:0 status not_scored - no fabricated per-skill verdict, no self-contradicting fraction) but the thread had no Housing section since 07-21 so it read as open. Its process point ("deployed build AHEAD of committed source") also closed: the cause was index.html loading every script with NO ?v= query, fixed r92.
  - DISCHARGE STAMPS applied 2026-08-02 to four threads whose Housing ask was already met: live_round2 (multi_select built), SpellingAuthor (error_id served r110), reporting_prisms (A6 shipped r110), PronounAuthor order_imagination_battery (unblocked by multi_select). Stamping matters: an unstamped done thread reads as work to a fresh seat.
  - r112 ACCENT DOCKING shipped (design_overhaul_reconciliation absorb; Smith reversed never-dock/0.9-flat). wrong_kind 0.8, omitted 0.5, correct 1.0, markpoint carries docked_from so a docked partial is separable from a real one. COULD NOT have been built before r107: docking needs to know WHICH KIND of error, and the drill previously fired the declared bucket regardless of the pick. Swept 276 items x every choice: 276 at 1.0, 218 at 0.8, 83 at 0.5. OPEN TO ARCHITECTURE: `inserted` is UNRULED - scored with wrong_kind at 0.8 and flagged rather than silently invented.   [shipped r112]
  - design_overhaul_reconciliation FOLDED at v2: coverage canvas / join-key analysis / hover-fix all already built; error_id built AND now actually served (r110); STR-ACC has real axes since r107; residue engine still awaits Architecture go. Correction lodged: this thread signed off the accent typing convention as "input layer, fine" when the 3-apostrophe escape had NEVER worked - that is how a dead feature stays dead. REMAINING: drill-terminology copy pass (wants Smith words) + the STR/ACC chip surface.   [inter_chat v2]
  - r111 "ALSO ACCEPTED" echo fixed (Vocab marker_semantics v6, Next:Housing, NEW 08-03). Architecture blamed the missing `alternatives` field; true but incomplete - acceptableList already splits the target on , ; / so the line fires anyway, and the display filtered the learner's own answer with a RAW lowercase compare while the marker matches with norm() + leading-article tolerance. "state" vs accepted "the state" survived and got echoed back. Key is now accent-folded + article-stripped and also drops anything equal to the `expected` shown above. 4/4 harness.   [shipped r111]
  - r110/r111 GENDER DRILL: Vocab gender_class tags (201 entries, 08-03) are consumed with ZERO Housing change - nounGenderClass already prefers the tag. Verified over 9,806 nouns: all seven classes drillable (1:5777 2:3736 3:242 4:29 5:2 6:2 7:18). FLAGGED BACK to Vocab: class 5 derives ZERO because every -a-plural noun was tagged class 7 incl. uovo/dito which have no ordinary masculine plural - a lexicographic call, theirs not mine.   [inter_chat gender v6, Next: Vocab]
  - METHOD FAILURE, corrected 2026-08-02: my triage swept `*Housing*.md` FILENAMES, so every thread addressed to Housing but living under ANOTHER chat name was invisible. Re-swept by reading the Next: line of all ~60 threads. Found: SpellingAuthor_batch_delivery (1-line error_id ask, standing since 07-21), MisconceptionAnalyst reporting_prisms (A6/A7), PronounAuthor order_imagination_battery (dischargeable now multi_select ships), Vocab marker_semantics v7 (also-accepted display suppression, NEW 08-03), gender_plural_drill v5 (Vocab gender_class tag LANDED 08-03). Sweep on the Next LINE, never on the filename.
  - r110 A6 TENSE CONFUSION MATRIX BUILT (replaces the placeholder): rows=chose, cols=correct, toggles for counts/row-% + transpose + hide-pairs-under-5; diagonal on the coverage green ramp, off-diagonal on a SEPARATE terracotta ramp (different class of quantity, never shares a scale), no red, angled bottom-seated column heads. Recovers legacy attempts with no chosen_tense via the 2-candidate fallback; anything unattributable is excluded and COUNTED in the caption. Harness verified incl. the legacy path.   [shipped r110]
  - r110 SPELLING DECK now serves error_id: buildSpellingDeck filtered type==="mcq" only, so SpellingAuthor 17 error-identification items delivered 2026-07-21 had NEVER been served. Choice synthesis extracted to prepErrorIdItem() and shared with the grammar path; deck 235 -> 252.   [shipped r110, discharges the SpellingAuthor 1-line ask]
  - r109 CHOSEN_TENSE recorded, unblocking the A6 confusion matrix (MisconceptionAnalyst bespoke_grid_specs; Architecture ratified the enabler 07-27, never built). store.js recorded candidate_tenses/correct_tense/was_right only, with a comment saying chosen-wrong-tense needed tense-tagged must_not_include entries - there are ZERO such guards in the content and all 199 tense-choice items are free-text, so that route was never going to open. Now read off the PRODUCED FORM via morph-it: data/it_verb_tense_forms.json (15 KB compact, generated by tools/build_tense_forms.py, re-run when new candidate_tenses items land). Compounds detected as aux + participle-by-ending so they work for ANY verb; simple tenses from the cued 19. Ambiguity resolved against the ITEM candidates; 0 or 2+ matches records NULL not a guess. Harness 12/12 incl. accent-free typing ("c e stato" -> passato_prossimo). SCOPE CALL ON RECORD: widening to all verbs at rank<=4000 measured 651 KB compact vs 15 KB, buying only a reading for off-task verbs - declined.   [shipped r109]
  - A6 confusion matrix (rows=tense chosen, cols=tense correct, toggles count/row-% + transpose) is now BUILDABLE - the axis exists. A7 piacere grid still needs verb_identity on family_verbs items (Architecture ratified adding it 07-27; NOT in the data yet - checked). B6 waits on MisconceptionAnalyst marking the mirror_of pairs.   [Next: Housing A6; Architecture/authors for verb_identity]
  - STALE NEXT (again): vocab_category_picker header says "Next: Housing (render fix + picker rebuild)" but its own v2 says all four faults shipped r85 and catSel multi-select is live in app.js (verified). Awaiting Smith live-verify only.   [derived 2026-08-02]
  - r107 ACCENT EVENT AXES (accent_stress §1, ratified 07-21, never built): every attempt now emits accent_type / expected_mark / written_mark / placement_class / outcome_class / pron_effect, ALL DERIVED from data already present (subtopic + choice_tags[i].class + a diff of correct-vs-picked). On a miss the leaf RETARGETS to what the learner did (omitted->.missing, wrong_kind->.wrong_mark, inserted->.added), declared_bucket preserved. CORRECTION TO MYSELF: from 2 samples I concluded every item declared wrong_mark and the axis had collapsed; counting the file says declared buckets are already varied (missing 141/added 81/wrong_mark 54) and the real mis-attribution is 25 of 301 distractor picks = 8%. The bigger half of §1 was that NO axes were emitted at all. Verified 276 items x every choice, 0 landing outside the tree.   [shipped r107]
  - r108 BAND LEGEND (accent_stress §4): per-row headers cannot fit 7-9px segments, so one legend at the top of the live panel names the order once (bands: io tu lui noi voi loro). The person x tense and person x class grids already had full headers.   [shipped r108]
  - STALE NEXT LINES: accent_stress §3 (error_id qtype) and live_round2 ask 4 (multi_select) were BOTH already built (2026-07-22) while their thread headers still said Next: Housing - a fresh seat would have rebuilt them. error_id: 17 items live in orthography. multi_select: buildMultiSelectResult live, credit = (correct-wrong)/total, Rev-27 cross-credit off choices[].buckets, 6 items in pronoun. Asked Architecture for per-SECTION status on multi-section threads.   [derived 2026-08-02]
  - r104 PHANTOM VOCAB BUCKETS (Smith live: "what is all this orecchio proprio stuff"). buildBucketContext lemmatised it_en source words by GUESSWORK - "first 3 chars match and lengths within 4" plus "one is a prefix of the other". Reproduced exactly: ORE -> orecchio (ear), PROBLEMI -> proprio. Those were injected and came back not_attempted, writing vocabulary events against words never in the sentence. Replaced with a real lemmatiser: data/it_surface_to_lemma.json, generated offline from morph-it (already in the repo) and restricted to surface forms occurring in translation source texts = 8 KB not 3.9 MB. Exact -> accent-folded -> morph-it, NO fuzzy fallback (an unlemmatisable word gets no bucket; a guess invents a fact). Verified: ore->ora, problemi->problema, potevo->potere. ARCHITECTURE: already-landed pulse rows carry phantom vocab events.   [shipped r104]
  - r104 "1Correct/2Incorrect" on the accent drill: NOT a score. The judge-the-form items have literal choices "Correct"/"Incorrect", and .accent-choice .gc-num had NO css rule (gender/spelling/stress choices all do), so the number inherited the button font size with no margin. Pure CSS omission, fixed + accent drill type scaled up (prompt 26px, choices 30px) so the marks are visible.   [shipped r103-r104]
  - r103 STRESS DRILL MEANING (Smith: "sometimes it does not tell you the meaning"): exact-lemma-only found 11,475 of 12,446; widened with accent folding + clitic stripping (eccolo->ecco) + apocope (ciascun->ciascuno) + inflection (qualcuna->qualcuno) = +59. The remaining 912 (7.3%) are a VOCAB DATA GAP not a lookup gap: 3,384 of 18,071 vocab entries (19%) have translation_en null, all in the deep tail. Route to Vocab.   [shipped r103, Next: Vocab]
  - r102 CUE RENDERER rebuilt from a corpus measurement (1,606 items carry a trailing parenthetical, 880 distinct). The 07-22 attempt guessed "single word + ends in a vowel + not on an English blocklist" - says yes to late/alone/inside, no to nothing it was not told. KEY FINDING: the dominant unhandled shape is NOT the English gloss the thread assumed, it is the COMPOUND cue (essere, futuro / avere, io / vendere, passato prossimo) - hundreds of items that would have rendered "Meaning: essere, futuro". Now 5 classes: operand 1408 (124 of them "Verb:" per v2), gloss 148, context 40, label 9, drop 1. Italian by POSITIVE evidence only (lemma/plural/infinitive/clitic-stripped pronominal/proper noun). My own first cut matched /auxiliary|agreement/ which also matches adjective_agreement, so EVERY adjective cue read "Verb: rosso" - the corpus caught it in one run. Also found: English words (well@14045, early@16248) sit in the Italian vocab frequency list with null translations.   [shipped r102, inter_chat v3]
  - r101 APOSTROPHE ESCALATION shipped (AccentAuthor thread OPEN since 07-21, now v2). KEY: the 3-apostrophe escape was ALREADY in the file and had NEVER worked - the rule list is applied IN ORDER so the escape was undone by a later rule in the same pass; only a PROGRESSIVE-TYPING harness exposed it (one-shot tests hide it, and the accent bar was advertising the dead feature). Now one atomic pass, 1=grave 2=acute 3=literal for every vowel; APOCOPE_IMPERATIVES + sentinel retired so da-grave/di-grave are typeable at last. Also: the rewriter fired on PASTE (corrupting correct pasted text) - now gated on inputType insertText with an apostrophe; plus an ADVISORY nudge (never rewrites) on forms where the grave is never correct. Open to Architecture: reserve-three (va/fa/sta) or reserve-none.   [shipped r101]
  - r99 ARCHITECTURE v19 RULINGS 2/3/4 SHIPPED to the worker prompt (Italian dictionary lemma only + no-lemma-no-bucket; proper nouns excluded; .passive is it_en-only, en_it uses bare .translation). R1 (flag temp-1.0 events, do not delete) NOT built - awaiting the provenance-flag spec; recommended the WORKER write the flag since the cut-off is per-learner-browser not wall-clock.   [inter_chat v20, Next: Smith deploy + Architecture]
  - r99 SCROLL, the general fix: FIVE sites did `ensureCardVisible(card); card.focus()` with no preventScroll - the bare focus makes the browser scroll AGAIN to reveal the focus ring, so a tall card (one carrying a result panel) nudges the page down and leaves the next question above the viewport. That is the gender drill AND spelling/accent/stress. Also the 2 choiceButtons focus calls and focusGrammarInput/focusTranslationInput. Rule now noted at ensureCardVisible: pair it with preventScroll ALWAYS.   [shipped r99]
  - PREFLIGHT EARNED ITSELF: writing the ruling text I put raw backticks in the prompt literal AGAIN (the 07-29 outage cause) and preflight failed the build before bump_build stamped an id.   [2026-08-02]
  - r97 BENCH aggregate was BLENDING FOUR DIFFERENT ITEMS into each row (grouped on mode+model+temp but NOT item) - every number in that table was meaningless, a 1-bucket and a 6-bucket item sharing a "skills returned" mean. Now one table per item+answer. Second time this class of error bit in one session; group keys must name EVERY thing that varies.   [shipped r97]
  - r98 VOCAB DRILL SCROLL (Smith live): pressing Enter nudged the page down, mostly on wrong answers. Cause was NOT vocab code - renderLiveStats calls scrollToFreshBuckets on every mark, but the vocab tab HIDES #live-stats (body.strand-vocab-active), so freshScrollBox finds nothing scrollable and the code falls through to the WINDOW branch. Guarded at source + a zero-rect/offsetParent guard + focusVocabInput now preventScroll.   [shipped r98]
  - MARKER DATA BUGS seen live, for Architecture: vocabulary.it.SPEECH.translation - an ENGLISH lemma inside the Italian vocab namespace (should be discorso); proper nouns (marco, anna) getting vocab buckets; same skill under both .translation and .translation.passive; and a 2nd 581-menu false positive (preposition.discrimination.di_vs_da on "avanti"). The false positives are the one menu argument that is NOT a sampling artefact.   [inter_chat v16-v18, Next: Architecture]
  - DeepSeek via DeepInfra returned 429 rate-limited mid-sweep (shared free routing). Operational argument for benching grok-4.3 / gemini-3.1-flash-lite, or adding an own OpenRouter key.   [Smith]
  - r96 TEMPERATURE: the marker never set one, so every mark ran at the provider default 1.0. Bench proof: 3 runs/mode, SPREAD 5-6 on a MEAN of 4.7 skills - variance exceeds the signal, full and labels both average 4.7, so v16's "agg_labels wins" is inside the noise. Now DEFAULT_TEMPERATURE=0 + optional seed + bench override. EVERY AI-marked translation event ever written carries this noise - Architecture ruling wanted on the historic rows.   [shipped r96, inter_chat v18, Next: Architecture]
  - r96 also: malformed_response now reports upstream error/finish_reason/reasoning-only instead of a bare "missing content"; a failed call no longer aborts a 30-call sweep; the aggregate table groups by mode AND model AND temperature (it was blending models).   [shipped r96]
  - r95 WORKER BUILD WAS BROKEN since commit 40e0295 (2026-07-29): rule 13 wrote raw backticks inside the system-prompt TEMPLATE LITERAL, so wrangler deploy could not build. FOUR DAYS of "needs a deploy" were undeployable; deployed worker is at best 86df99a (07-22). Escaped, builds green. tools/preflight.py now node-checks every js + inline script block, greps the prompt literal for raw backticks/${, and compiles the worker WITH ITS REAL EXIT CODE (the old check piped esbuild into tail, which always succeeds). bump_build.py refuses to stamp a build that fails preflight.   [shipped r95]
  - MISSING GREENS had THREE causes, not one: (1) menu 136->581 client-side, (2) v6 en_it vocab never built, (3) worker uncompilable since 07-29. All three now addressed on disk; (2) and (3) need the deploy.   [shipped r93-r95]
  - BENCH EVIDENCE r93: agg_labels 5/5 correct at 5,775 tok/$0.0022 vs full 3/3 at 56,013 tok/$0.0157 WITH a false positive (pronoun.indirect_object fired on a sentence with no IOP). BUT aggregates and retrieved sent byte-identical payloads and returned 1/2 vs 2/4 - run-to-run variance ~= between-mode gap, so bench now does N runs with a SPREAD column. Do not rule the menu until spread is small vs the gap.   [inter_chat v16, Next: Architecture]
  - LATENCY tracks OUTPUT tokens not input (304 out=4.8s ... 852 out=57.8s; the 56k-input run was FASTER than the 5.7k one). Breadth costs latency directly; the lever is a faster model, not a smaller menu. 9 current OpenRouter ids added to MODEL_PRICING + picker.   [shipped r95]
  - DATA QUALITY from the it_en runs: same skill recorded under BOTH vocabulary.it.X.translation and .translation.passive (coverage will split - verify resolveVocabInMarkpoints runs on the live path); and proper nouns (marco, anna) are getting vocabulary buckets.   [inter_chat v17, Next: Architecture]
  - r92 CACHE-BUSTING: housing/index.html loaded ALL its js with NO ?v= query, so browsers served stale JS after every push - the likely root of three separate "is it deployed?" arguments on the record, incl. crosstopic v14 declaring a pushed build unpushed. Every js/css asset in index.html + bench.html now carries ?v=<build>, and tools/bump_build.py bumps LL_BUILD and the queries together. A stale-asset symptom is now a BUG, not an explanation.   [shipped r92]
  - r91 MENU MODE + bench: push, then run bench.html "Run every menu mode" on the failing item; report which modes restore the greens   [acceptance, Smith]
  - v6 en_it production-vocab exception: NEVER BUILT since 2026-07-21; blocks ALL vocab greens on en_it; needs a worker prompt change + deploy   [inter_chat, Next: Architecture re-route, then Housing]
  - v9 prompt-cache: never built; bucket_context must move ahead of the per-item block in buildUserMessage before any prefix cache can bite   [inter_chat, Next: Architecture]
  - rule 14 (clitic placement vs form) sits UNCOMMITTED in worker/src/index.ts; ship it in the same deploy as the v6 vocab exception   [external-action, Smith]
  - DERIVED 2026-08-02: the handover task list was NOT the queue - NINE threads sit on Housing (apostrophe_input, accent_stress §1/§3/§4, cue_notation_renderer, multi_select, vocab_category_picker, gender drill, bespoke grids, design_overhaul fold)
  - DISCHARGED 2026-07-18: worker deployed by Smith (wrangler output on record); accent policy live-verified on a real attempt
  - push r52 (SPAM-to-green exploit closed + hover trimmed; design fold r44-r51 + reskin); answer one item; extra_json column should populate (if still empty, ball to EdTech with evidence)   [external-action + acceptance, Smith]
  - RESOLVED 2026-07-21: 13/23 verified + relabelled; BOTH shade cells now deleted (green r32, blue r34)
  - misconception tagging rollout beyond tense_choice/imperativo/imperfect + 'answer' as estate column: flagged to Architecture   [inter_chat, Next: Architecture]
  - misconception_event_keys v2: JOIN KEY shipped r36 (attempt_id on row + per-object FK); recommended joining over denormalising person/strand onto each event; schema call is Architecture's (on-row blob vs separate misconceptions tab)   [inter_chat, Next: Architecture]
  - accent viewer: awaiting Architecture's spec (strip's accent box is the planned entry point)   [inter_chat, Next: Architecture]
  - DISCHARGED 2026-07-23: stress drill built r77 (load path + tap-qtype renderer + STR strip box + 4x4 confusion matrix + pulse keys); needs push
  - cue_placement: rename DONE r16; awaiting Architecture's 1,665-item key migration, then ExistentialAuthor's ex_form_neg_01 unset   [inter_chat, Next: Architecture]
  - also_credits markpoint field - future Housing pass   [OPEN_QUESTIONS, parked]
  - whole-answer anchoring match_at "exact" - future Housing pass   [OPEN_QUESTIONS, parked]
  - live-verify r28: person STACKS read right at 20px? drill re-click steps out; drilled outline; streak grid quiet enough   [acceptance, Smith]
  - RESOLVED 2026-07-18: row-height question settled by keeping BOTH as toggleable views (r22)
  - RESOLVED 2026-07-18: scoring ruling arrived (denominator floor) and shipped r23
  - residue engine build: awaiting Architecture's go + 2 rulings (all-matches subtraction; whole-prompt licensing)   [inter_chat, Next: Architecture]
  - DISCHARGED 2026-07-20: person-split Housing half shipped r26; residue = authors' 163 backfill + Smith's surface reaction
  - DISCHARGED 2026-07-18: site pushed through r16 (repo 0/0 vs origin), Apps Script re-deployed, feedback.js carries the real endpoint - feedback delivery live
