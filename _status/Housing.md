seat: Housing
project: Linguics
updated: 2026-07-21
waiting: Smith push r64 + next vocab cluster   # r64 shipped (vocab perf + vanish + top-chrome cleanup); worker now deployed by Smith so cross-topic marking is LIVE
needs_from_smith: external-action
blocked_by:
claude_can_verify: push + deploys only Smith can do; the pronoun-tile scroll and cold-load timing only Smith's browser can confirm
summary: r64 VOCAB PERF + VANISH FIX (Smith direct). SLOWNESS root cause: the per-answer FULL heatmap rebuild. doMark called renderVocabAxes() (teardown+rebuild of ~990 focused dots + ~20 flanking blocks, each dot an eventsForEntry lookup + click listener) on EVERY mark, and renderVocab() rebuilt it again on EVERY Next. NOW: doMark recolours ONLY touched cells in place via new recolourAxisCellsFor(entry) (focused dot + its flanking cell + gender/theme lemma dots; sets SETTLED bg, not just a flash); renderVocab(keepAxes) + doNext->renderVocab(true) skips the rebuild on advance; full rebuild now only on strand-enter / filter change. VANISH: focused-grid hits already persisted (scrittore: 1 event->1.0->dark green); the 'flashed then vanished' was FLANKING cells - a cell aggregates ~9 lemmas so one hit dilutes to ~0.29, and baseline==yellowStop(0.2) made touched read as untouched (disponibile). FIX: recolour sets the true aggregate AND adds .has-practice (inset green border) to any flank/lemma cell containing practice; also added in renderFlankingStripes so it survives full rebuilds. CLEANUP: removed 'Vocab practice' h2 + the redundant note <p> (dup of card hint + a 'translation only' placeholder); removed the 'Yellow stop' dev tuning input (var kept, default 0.2); lowercased the 2nd 'enter' in the card hint (2 sites). node --check clean, css balanced. WORKER now deployed by Smith (wrangler x3, live ver 9c83f0d4) so r63 cross-topic translation marking is LIVE. DEFERRED vocab work-order (Smith brain-dump, queued, need push+decisions): strip demarcation + align Restrict-to/Sub-band labels; direction sticky top-right + 'translation only' (needs ruling on removing EN->IT/mix option); theme picker w/ family grouping+hover+select-all (check Vocab taxonomy dep); drill-in no-way-back / can't-unclick; drill entries for Gender/Accent/Stress + back-to-front-page (Stress unwired); remove rest of palette-tuning row? NEEDS r64 PUSH. PRIOR: r61 + WORKER FIX - the AI marker IS working; the red box (r61) revealed the real cause: DeepSeek wraps its JSON in a ```json ... ``` markdown fence, and worker/src/index.ts did a BARE JSON.parse(content) -> malformed_json -> housing fell back to the stub. Fixed worker/src/index.ts (~L451): strip the code fence, and as a fallback extract the outermost {...}, before JSON.parse. CRITICAL: this is a WORKER change - needs `wrangler deploy` from the worker/ folder (or the EdTech/worker owner), NOT the GitHub push that deploys the housing site. After the worker redeploys, the real per-skill AI marking fires. r61 surfaced the failure reason (was erased by resultHost reset). r60 stats-pane top pinned + last-10 coloured. r59 re-mark exploit locked. PRIOR: AI TRANSLATION MARKER 'not firing' diagnosed + partly a silent-failure bug of MINE: when a worker URL is set (toggle 'on'), markTranslationLive IS called, but on ANY error the catch wrote a 'Live marker failed' note that the very next `resultHost.innerHTML=""` ERASED before render - so the AI was failing and the learner only ever saw the stub ('real AI marker will give per-skill feedback'), silently. Now the failure reason is CAPTURED (aiError) and shown above the stub result in a red .marker-ai-error box. So on next push+mark, Smith will see the ACTUAL cause (network / HTTP / worker error) - likely a worker deploy or URL issue, which is worker/EdTech-side not Housing code. r60 stats-pane top pinned + last-10 coloured. r59 re-mark exploit locked. PRIOR: live-review batch. r59: RE-MARK EXPLOIT closed - doMark had no `if (marked) return`, so you could change your answer and press Mark again to overwrite a miss with a hit (and the r52 dedupe kept the latest, helping the cheat); now guarded + input goes readOnly on commit; second-chance path unaffected (marked still false during it). r60: STATS-PANE TOP pinned - the vocab pulse strip now mounts in #live-stats .live-header (was top-left of the drill), the last-10 row moved into the header, the whole .live-header is position:sticky so it doesn't vanish on scroll, and the last-10 ticks are coloured (green hit / red miss). r58 hover trims (tree-row description dump + Drill-into-this gone; slivers get leaf context). r57 agreement cells light (non-person sub-aspects get plain fill not empty person barcode). r56 mini-cell sub-aspect labels. PENDING from the pile: identity strip (Smith·Test) above the header line. STICKY/layout wants Smith's eye on push (blind CSS). PRIOR: RESIDUE ENGINE shipped (marking absorb 2/3, extraneous_word_residue v3 GO): single-markpoint non-MCQ answers containing the positive PLUS extra unlicensed words now score a NAMED MISS. residue = norm(answer) MINUS all matching any_phrases (all-matches) MINUS whole prompt tokens (blanks/brackets/parens stripped); harness-verified w/ REAL norm on the thread's table. Multi-markpoint untouched; soft second-chance banner not yet wired. r54 - accent DOCKING (0.5 omission / 0.8 wrong-kind, supersedes never-dock) + quick-start rename 'Work on recent weaknesses'. r53 - Stuck?Help moved under the question right-justified. r52 - spam-to-green exploit closed + hover trimmed. REMAINING absorb: error-ID qtype. Polish: corner streak, segmenter colon, welcome coverage taste. PRIOR: TWO Smith-flagged bugs fixed. (1) SPAM EXPLOIT closed: recencyWeightedCorrectness weighted every attempt, so repeating one question stacked weight past the 9.5 two-for-green floor -> dark green. Now rawEventsFor tags each event with its source item and recencyWeightedCorrectness dedupes to the most recent event per (item,bucket) before weighting, so mastery reflects DISTINCT items. Harness-verified w/ real constants (W_LAST 7/W_PRIOR 3/floor 9.5): spam 1 item x5 -> 0.737 (proficient, NOT dark), 2 distinct items -> 1.000. (Vocab path via eventsForLemmas untouched - events there lack .item so dedupe skips them; follow-up if vocab spam matters.) (2) HOVER trimmed: friendlyOverviewTitle now = label + question-shaped counts only, dropped the full bucket-description paragraph + 'Drill into this' (Smith: 'hover text is mad - either full paragraph or just io'). Person-band slivers already show 'io: X% (n attempts)'. r51 - design structural fold complete. r48 analysis 3-tab. r44 wine. r43 - design STRUCTURAL fold essentially COMPLETE. Welcome-splash now has per-strand analysis links ('full grammar analysis ->' opens Analysis on Grammar tab, 'full vocabulary analysis ->' on Vocabulary) replacing the single 'See your analysis'. Bucket-tree browser DROPPED from the top nav (design: fold into Analysis) - now reachable via a 'Browse the atomised bucket tree ->' link inside the Grammar analysis canvas. Design fold done across r44-r51: wine reskin, logo, analysis 3-tab (wired for real r48), clear-palette default, mobile bottom tab bar, sticky 'Linguics' header, feedback lift, welcome links, bucket-tree fold. REMAINING = marking-side ABSORBS (own round, they change SCORING): residue engine (additive-word named miss, single-markpoint, soft 2nd-chance), error-ID qtype (pick the wrong word, index-scored, over-regularisation distractors), accent DOCKING (0.8 wrong-kind / 0.5 omission, supersedes 0.9-flat). Finer: corner streak on practice screen, GEN/STR/ACC condensed rail. r50 sticky header. r49 mobile tabs. r48 analysis 3-tab. r44 wine. r43 - HEADER now STICKY (position:sticky top:0) so 'Linguics' (logo+wordmark+nav) is permanently there when scrolling on desktop AND in analysis (Smith: 'need linguics permanently there'). Feedback pill: already position:fixed bottom-right in feedback.js (NOT forked) so it stays on-screen on desktop; lifted above the mobile tab bar via CSS override (was going to overlap). If Smith still sees feedback off-screen in a specific view, need that view. r49 - MOBILE BOTTOM TAB BAR. NOTE live is r44+ (Smith pushing); STOP parroting stale live numbers. r48 analysis 3-tab wired for real, r45 logo, r44 wine reskin. r43 - MOBILE BOTTOM TAB BAR shipped (design overhaul): fixed-bottom Grammar/Translate/Vocab/Analysis on phones (<=600px), top strand-nav hidden there, wired to showStrand/showCoverage with active-state sync. NOTE Smith corrected me: live is r44 not r32 - I'd been parroting a stale build number every turn; STOP asserting live state, derive it or omit it. REMAINING design structural: welcome-splash per-strand analysis links; move bucket-tree browser into Analysis; (streak-in-corner, condensed GEN/STR/ACC rail = finer). Absorbs still pending (marking-side): residue engine, error-ID qtype, accent docking. r48 - DESIGN OVERHAUL Phase 2 (structural) + a REAL BUG I owned: the r40 analysis-scaffold wire-up had SILENTLY FAILED (un-asserted .replace missed) - buildAnalysisHeader/renderMisconceptionCanvas/buildKpiStrip were DEFINED but never CALLED, so the switcher/KPI/Misconceptions-tab never rendered (only the coverage matrix + person grids did). Now wired for real (every edit asserted). Analysis surface is now the design's THREE tabs: Grammar (KPI snapshot + coverage matrix + gaps + person×tense/×class) / Vocabulary ('to be developed' placeholder) / Misconceptions (top errors + family heatmap + lens strip + placeholders), with a '← Back to practice' exit. Coverage palette A/B TOGGLE RETIRED - clear is the permanent default (Smith/design). r47 - wine accent nudged #8f3a44->#8f2b45 (less brown). r46 - CEFR now re-scopes the panel (renderLiveStats added to filter onChange). REMAINING design: mobile bottom tab bar; welcome per-strand analysis links; move bucket-tree into Analysis. r45 logo, r44 wine reskin P1. r43 - FIXED: selecting CEFR now re-scopes the coverage panel (Smith flagged it twice). Root cause: the grammar filter bar's onChange re-rendered the DECK but never called renderLiveStats, so the panel only re-scoped on Mark, never on a level toggle - my r37 rootInLevelScope guard was correct but never re-ran. Added renderLiveStats() to onChange. NOTE the scope test is core+preview, so A1 keeps things PREVIEWED at A1 (e.g. passato prossimo); advanced tenses (congiuntivo/passato remoto/pluperfect) drop. If Smith wants a harder cut (core-only), it's a one-line change to rootInLevelScope. r45 - knowledge-atom LOGO added (3x3 wine L-trace in header + favicon.svg). r44 - DESIGN OVERHAUL Phase 1 wine reskin (--accent slate->wine across 94 uses, Fraunces+Lora, lingu·ics wordmark, RWG ramp kept semantic). r43 - DESIGN OVERHAUL Phase 1 (Claude Design wine reskin; Architecture_Housing_design_overhaul_reconciliation says NO hard conflicts, fold the visual over the still-valid engine). Brand accent slate #3a4f8a -> WINE #8f3a44 across all 94 var(--accent) uses (0 raw slate left) + wine ramp tokens --p..--pDeep + CEFR tints re-tinted wine; Fraunces headings/wordmark + Lora body via Google Fonts; wordmark 'lingu·ics' (ics in wine); 'housing/prototype' chrome dropped, Vocab->Vocabulary. RWG mastery ramp kept SEMANTIC (not wine-ified) per spec. NOTE much of the design's STRUCTURE already shipped this session (analysis surface r40-42, clear-palette r39, question-shaped counts r36, no only-touched r37, person bands). REMAINING design phases: analysis as 3 full-width tabs (Grammar/Vocab/Misconceptions) + 'Back to practice'; coverage clear-palette as DEFAULT (retire A/B); mobile bottom tab bar; knowledge-atom logo; welcome per-strand links. ABSORBS (marking-side, separate, additive): residue engine, error-ID qtype, accent DOCKING (0.8 wrong-kind / 0.5 omission - supersedes 0.9-flat). WANTS a push + Smith's eyes (fonts/wine/oklch unrenderable here). r43 - CARD partial (Smith's confirmed subset): (1) the 'Use' cue label now reads 'Register:' for MULTI-WORD cues only, so context/register cues get it while verb cues like '(avere)' stay 'Use'; (2) choose-or-type chips are now STACKED and NUMBERED 1/2 (badge); (3) the accent bar is DROPPED whenever choice chips are present (was hidden only in select mode). DEFERRED (subtle, want a real render): the segmenter colon-gap and an explicit 'Complete with one of these' instruction line. STILL QUEUED: panel-top (vocab strip pinned to top of stats panel + 'Linguics' brand fixed + streak surfaced + level control) - sticky CSS, best verified on a push. r42 - THREE more analysis views live + registry wired. Loader now pulls data/misconceptions.json (17 families + 81 specifics) and data/misconception_lenses.json into LL.misconceptions/LL.lenses. Canvas A: person x CLASS grid live (io..loro x conjugation classes from the event 3rd-segment). Canvas B: FAMILY HEATMAP live (17 families as error-count bars; family = misconception-id prefix, harness-verified) + SURFACE-LENS STRIP live (8 lenses from the lens file, count = sum of member-family hits, verified). Remaining placeholders (6) are genuinely upstream-gated: tense-choice confusion matrix (needs Phase-2 chosen-wrong-tense tagging), piacere grid (needs analyst bespoke spec); cross-kind / region x family / over-under (mirror_of) / strand (v15 comprehension) - all Architecture/analyst data. So the buildable-now analysis views are DONE; natural point to pivot to panel-top + card. r41 - IN-PLACE CELL DETAIL shipped (Smith 'we don't want to leave this page'): clicking a coverage cell now opens an on-page panel (the topic's areas + correctness, misconceptions logged in the topic, opt-in 'Practise these ->' button) instead of jumping into the drill -> task 14 (coverage findability + no-leave) COMPLETE. PERSON x TENSE grid now LIVE on Canvas A: io..loro down x 9 tenses across, cells coloured by correctness from person-tagged verb events, sparse-friendly (empty = unpractised), aggregation harness-verified. Remaining Canvas placeholders (next): person x class, tense-choice confusion matrix, piacere; family heatmap, surface lenses (needs lenses.json load), cross-kind, region x family, over/under, strand. r40 - ANALYSIS SURFACE scaffold shipped (Smith: 'yes, analysis surface'). The coverage view is now a two-canvas Analysis surface: Coverage <-> Misconceptions switcher, each an ordered vertical SCROLL of view-sections (analyst's data/misconception_canvas_views.md). LIVE views - Canvas A: Snapshot KPI strip (answers / fully-right / mean-credit), the coverage matrix (reused, palette toggle intact), Not-yet-attempted gaps; Canvas B: Top recurring errors (counts whatever misconception tagging exists - thin now, fills as it lands, per Smith 'data catches up'). PLACEHOLDER sections (build-order-gated NOT data-gated, turning live next): personxtense, personxclass, tense-choice confusion matrix, piacere; family heatmap, surface lenses (needs lenses.json loaded), cross-kind, regionxfamily, over/under, strand. FINDABILITY (task 14): persistent 'Open analysis ->' button in the stats panel + 'See your analysis ->' on the welcome screen; STILL OPEN - matrix cell click still calls coverageDrill (leaves the page), in-place cell detail is next. Mobile-considered CSS (switcher wraps, sections stack, matrix scrolls-x); general mobile sweep queued (task 16). KPI+top-errors counting harness-verified, node clean. r39 - coverage PALETTE TOGGLE wired (Smith declined a local server: 'wire it up with a toggle, get rid of it later'): default 'clear' = in-scope-unpractised WHITE so partial coverage stops reading as empty + non-existent gaps AND unobtainable cells solid GREY #d9d6cc (same status, Smith's ask); toggles back to 'classic' cream to compare; description reflects the active palette. Verify on push, then drop the toggle & keep the winner. CONFIRMED, NEXT on Smith's continue (BOTH AT ONCE): (a) top-of-right-panel = vocab strip -> pinned top of stats panel + 'Linguics' brand pinned (never floats) + streak surfaced there + level control repositioned; (b) card = 'Use'->'Register:', options stacked & numbered 1/2, instruction line, fix segmenter colon gap, drop accent bar when choice chips present. r38 - coverage sediment now SORTS practised strips by correctness so the darkest green (best-covered) sinks to the very floor, red/low rides just under the unpractised 'to get' band (Smith). PENDING browser-verify (shade-subtle, want it rendered first): partial coverage reads as EMPTY because the rwg ramp is white at ~50% and meets the #faf8ef cream cell bg; sized-box unobtainable TOP (short box in a tall row) shows cream, must be GREY like coverage-na. PENDING Smith-confirm (UX, the important ones): coverage FINDABILITY - only reachable from the entry-screen 'See your coverage' link + stranded after refresh; and cell click calls coverageDrill (LEAVES the page) but should open IN-PLACE detail (misconceptions + attempted/available counts via nodeQuestionStats), relabel drill->in. r37 - removed the dead 'only touched' toggle (index.html + app.js, 0 dangling refs); LEVEL now thins the OVERVIEW TILES too - renderLiveOverview bypassed rootInLevelScope so the tree was scoped but the tiles were not (Smith: 'shows in the stats but they don't reduce'). PENDING Smith-confirm (design): vocab strip -> top of right panel pinned + streak surfaced there + level control repositioned (items cluster at the panel top); question-card redesign (segmenter colon gap, 'Use'->register label, stacked numbered options 1/2, drop accent bar when choice chips present). r36 - tile headline is QUESTION-shaped ("N attempted of M available questions" + "K correct out of N attempted"); the leaf-touched ratio + proficient count are dropped from grammar tiles (vocab/diagnostic tiles keep the leaf line, guarded on nodeQuestionStats.total==0). JOIN KEY shipped: attempt_id now rides every pulse row AND is embedded as an FK inside each misconceptions_json object, so any nested record joins back to the full row instead of copying its columns (answers Smith's "are they not joinable" - they are now). r35 - stats hovers now question-shaped ('N attempted out of M questions' + 'K correct out of N attempted', via a per-bucket item index w/ ancestor prefixes; diagnostic trees get an events fallback); per-row last-N ticks retired and replaced by ONE global 'last 10' report row above the overview (Smith's mid-turn refinement). r34 - extra_json was EMPTY in the live sheet (my v4 claim wrong, owned): estate script sweeps top-level keys only -> payload flattened (answer/expected lead, jsons pre-stringified); BLUE cell deleted (both shades now gone); Smith's contract registered: full fired-against record per row via markpoints_json; Architecture asked re untagged-wrong fallback. r33 - PULSE LIVE (Fletcher's 13 rows in the sheet); Smith's row-review shipped: raw+expected answers lead extra_json, [object Object] misconception-column bug fixed pre-fire (blanks = only 221/4944 guards tagged - Architecture's rollout), log-out (cohort-scoped) + gate name-prefill, tiles say 'N/M tried · K proficient' with 70% threshold stated. r32 - Smith's tidy-up round: strip reworked (5 gapless 1k boxes, 2px matched slivers, Vocabulary caption, ACC/STR-inert/GEN boxes, on translation too, u00b4 CSS-escape bug fixed), COMPULSORY light sign-in gate, sidebar level-scoping (vocab roots exempt), green cell retired, vocab restriction-focus + palette report removed, bigger level toggles; ONE clarify pending (13/23 counts vs person-band emptiness). r31 - grammar PULSE STRIP shipped (Smith-direct prototype): vocab rank mini-map (3 chunky 3x3 boxes = first 5k, 15 slivers to 20k, wash + live green/red fires, click->vocab coverage) + grammar & accent micro-boxes (accent: green on accented-right, amber on slip - the seed of the coming accent viewer). r30 - LOGIN + PULSE BUILT (pulse.js: shared identity key w/ preservation proof, scoped cohort, maximal extra_json at the store funnel, session_start, pill; classes Test/Y12/Y13 interim). Verification = Smith's first row in the linguics tab. r29 - live_round2: operand cues inline (segmenter line-grouping), accent slips = unmissable amber banner + fully-classed events (expected/written/class/chars - feeds pulse payload too); deck-order report does NOT reproduce (all 4 builds shuffle, repro requested); multi_select feasibility answered (cheap, 2 rulings needed). LOGIN+PULSE build planned, blocked ONLY on Smith's interim class list. r28 - Smith's live round on the barcodes: bands flipped to HORIZONTAL STACKS (dot 14x20), footer per-topic inventory retired, quiet 6x5 30-day practice grid + streak label (yesterday-grace rule, no fanfare), build id legible. r27 - paradigm bands GENERALISED (leaf-declared {field,slots,labels}, person fallback, >8 slots = grid, store carries person+slot) ahead of Architecture's migrations; drill-out fixed 3 ways (re-click steps out; out+undo always visible - labelless undo was missing, likely THE defect); drilled box gets stable outline. r26 - person-split Housing half SHIPPED: events person-tagged in store.js (absent stays absent, null carried), finite formation leaves = paradigm barcodes in overview dots + band strips on tree rows, per-band floored mastery (two-for-green per person); coverage matrix left whole-leaf (judged, on record). r25 - drill thread: tree rows were a PAINTED DOOR (cursor+hover, zero handler) now wired; vocab axes already drilled (corrected the record) + tooltips added; index.html EOL-normalised. Person-split migration DONE architecture-side - my half (event tagging + N-band render) is the next round. r24 - live-round UI thread closed same-day: vocab parenthetical inline (flex-column bug family), choose-or-type chips (prompt-stated sets only, 129 items; candidate_forms excluded to honour crit-16 suppression - flagged to Architecture) + Input preference, MCQ number badges. r23 - architect specs landed + acted on: denominator floor SHIPPED (9.5, opt-in per call site - kernel is shared with 4 vocab sites, scope correction on record) + residue survey DELIVERED (1,839 answers, ZERO flips both rules, instrument validated; awaiting go + 2 rulings). Person-split queued behind Architecture's migration. r22 - coverage views TOGGLEABLE per Smith's ruling: equal-boxes (his model, default) vs sized-boxes (r21), cream=unobtained / grey=unobtainable glossary, hatch retired; scoring-formula discussion Smith->Architecture pending. r21 - matrix legibility round: uniform gapless 4px liquid layers (contiguous colour at bottom), scope-scaled row heights, grey unpractised vs hatched NA, family subgroups; A2/B2 counts verified REAL from data. r20 - 'reduced coverage' = matrix rendered off SAMPLE tree during load window (transpose = re-render); coverage now load-gated + refreshed by the loader. r19 - coverage stripes horizontal + sediment-bottom fill + rwg colours (coverageColour had NO red - wrong answers were invisible in the matrix). r18 - coverage matrix cells de-aggregated per Smith (one strip per area, own colours, coverage = colour spread; dense topics band). r17 - Smith's live round: scroll CONFIRMED, pulse moved to the small cell/dot, in-session multi-level toggles (a1b1a2) + composed-scope preservation, accent policy CONFIRMED live (0.9, caffe named). r16 - cue_placement v3 ruling acted on same-day: field renamed wrong_answer_is_form_error_only (claim-named), engine read live with legacy tripwire, migration ball with Architecture. r15 - Smith-direct scroll regression fixed same turn (v7 scrolled the wrong scrollbox + flashed pre-scroll; now ancestor-walk box choice + scroll-then-flash, geometry-mocked harness). Previously: queue cleared at r14 - 8 threads closed/advanced today (scroll fix, entry load race, level picker set, AI-marker accent policy, vocab builder two-lens + ratified 94-chip category picker, cue placement CSS, feedback widget mounted); cue-placement FLAG ruling awaits Architecture
queue:                     # derived from the name-grep 2026-07-17, judged live-vs-historical
  - DISCHARGED 2026-07-18: worker deployed by Smith (wrangler output on record); accent policy live-verified on a real attempt
  - push r52 (SPAM-to-green exploit closed + hover trimmed; design fold r44-r51 + reskin); answer one item; extra_json column should populate (if still empty, ball to EdTech with evidence)   [external-action + acceptance, Smith]
  - RESOLVED 2026-07-21: 13/23 verified + relabelled; BOTH shade cells now deleted (green r32, blue r34)
  - misconception tagging rollout beyond tense_choice/imperativo/imperfect + 'answer' as estate column: flagged to Architecture   [inter_chat, Next: Architecture]
  - misconception_event_keys v2: JOIN KEY shipped r36 (attempt_id on row + per-object FK); recommended joining over denormalising person/strand onto each event; schema call is Architecture's (on-row blob vs separate misconceptions tab)   [inter_chat, Next: Architecture]
  - accent viewer / stress drill: awaiting Architecture's spec (strip's accent box is the planned entry point)   [inter_chat, Next: Architecture]
  - cue_placement: rename DONE r16; awaiting Architecture's 1,665-item key migration, then ExistentialAuthor's ex_form_neg_01 unset   [inter_chat, Next: Architecture]
  - also_credits markpoint field - future Housing pass   [OPEN_QUESTIONS, parked]
  - whole-answer anchoring match_at "exact" - future Housing pass   [OPEN_QUESTIONS, parked]
  - live-verify r28: person STACKS read right at 20px? drill re-click steps out; drilled outline; streak grid quiet enough   [acceptance, Smith]
  - RESOLVED 2026-07-18: row-height question settled by keeping BOTH as toggleable views (r22)
  - RESOLVED 2026-07-18: scoring ruling arrived (denominator floor) and shipped r23
  - residue engine build: awaiting Architecture's go + 2 rulings (all-matches subtraction; whole-prompt licensing)   [inter_chat, Next: Architecture]
  - DISCHARGED 2026-07-20: person-split Housing half shipped r26; residue = authors' 163 backfill + Smith's surface reaction
  - DISCHARGED 2026-07-18: site pushed through r16 (repo 0/0 vs origin), Apps Script re-deployed, feedback.js carries the real endpoint - feedback delivery live
