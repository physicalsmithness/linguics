seat: Architecture
classes: [all-seats]
project: Linguics
updated: 2026-08-14
waiting: active
needs_from_smith: the DEPLOY (r164 is committed and pushed; Housing needs it deployed before a reasoning model can be benched at all) + the worker URL config. NOT outstanding, contrary to two stale thread lines I repeated: "push r124/r126" — the build on disk is r164 and git is 0 ahead of origin. PARKED by Smith: prism pick; EdTech cap five.
claude_can_verify: yes — every figure below derived from disk on 08-14; the merge's seven gates re-run post-write; the sweep agent's five load-bearing claims hand-audited before use
summary: 2026-08-14 second pass. WITHDRAWN my own ancestor-drop rule of 08-13: it rested on `app.js:8051`, whose `getItemBuckets` reads markpoints/required_buckets and NEVER expected_buckets, and the marker consumes the fire-list flat — so an aggregate is credited only when claimed. 1,819 claims restored (mean expected_buckets 11.22). PronounAuthor then argued the replacement from its own 603 verdicts and I adopted its convention with a checkable amendment: an aggregate earns its place when its authored leaves do NOT exhaust the construction (residue test, auditable against the tree); `attributes.is_aggregate` is NOT the test (337 marked, 158 real). Staged to Rev 33. RULED: comprehension arm ADOPTED as `direction: "reading"` on the specific, not a new family — decided on the count (264 of 913 translation items are it→en, 29%), unblocking 16 DEFERRED entries; 6 negation specifics minted (registry v2, 112 entries) incl. both reading-side ids; phonology IS in coverage, counted as ATOMS not items per Smith's precept (31 cited buckets, StressAuthor owes labels, lexical_simple's 6,413 items flagged as arguably not one atom at all); AccentAuditor finding-discipline clause accepted to Rev 34; AccentAuthor's 306-item regen verified from disk and handed back. EARLIER 2026-08-13: EXECUTED the tier-2 merge: 21 packets, 6,145 verdicts, 2,697 grammar buckets into expected_buckets, mean 6.27 → 9.23, all 21 threads stamped CLOSED. Three packets (Pronoun/Indefinite/RelativePronoun, 985 verdicts) were MISFILED in the dispatch dir — a returned/-only merge would have skipped them silently; the tool now reads both. RULED: store the most specific claim, ancestors are derived and never stored (1,819 candidate additions dropped; settles Connective-vs-Preposition). FOUND: all 1,434 strikes were no-ops because Job 9 rebuilt expected_buckets the day AFTER the packets were cut, and it emits no grammar ids — 26 seats judged a field that had been rebuilt underneath them. HAZARD RECORDED: Job 9 is a replace not a merge and will wipe the 2,697 on its next --apply; warning written into the script head, standing order in DECISIONS. PRIOR: 2026-08-05. RULED from live-learner feedback: the selection-policy layer (one `selection` object across all five surfaces; five presets; announced exhaustion ladder), welcome_entry_screen v11 REVERSED (resume restores deck+position; its revisit clause fired), the item_ref identity fix (store drops all five vocab/drill card ids), four defects in the mistakes shortcut, and one statistics system for the drills. Written to inter_chat/Architecture_Housing_selection_policy.md v1 + mockup; every claim re-verified against app.js/store.js line numbers after writing. v2 same day: CORRECTED my own §1 on Smith's question (gender coverage IS kept — vocab bucket ids carry lemma/pos/gender/number and the parser whitelists aspect `gender`; the gap is the three authored-MCQ drills only), separated the item reading from the kind reading (the latter is the misconception axis, view missing not classifier), and ruled the weaknesses button into a generated panel (don't ask, show). PRIOR: 2026-08-03 third+fourth pass. EXECUTED: accent regen per seed_frames v7 contract (gate green, posted v8; AccentAuthor to accept). RULED: four direction-merged ids split (+doctrine); mirror_of ratified; seven negation families routed; neg_corr_06 declined (attested variation, 0.9 stands); interrogatives residue = docked partial (Housing 3-line fix routed); PP retired estate-wide; caldo/freddo noun stands + adjective siblings split; verb_identity landing plan (PiacereAuthor). CORRECTED: 08-02 zero-item ruling's example (outcome axes are marker-class-only; placement is item-bearing) + riders R5 + answer-leak owe-list (7 not 8). DRAFTED: R7 worker prompt edit. Cue residues routed (Pronoun/Adjective/Housing).
queue:
  - Expectation suite tranche 1 DELIVERED (14 cases). Tranche 2 waits on Housing's first run showing where the gaps are.
  - RETURNS QUEUE CLEAR: all 26 wave-1 packets returned and merged (21 on 08-13, the last five on 08-14: Possessive 84 claims, Demonstrative 74, Adverb 31, Passive 21, Comparison 10). Plus 6 of 8 search packets merged (Noun 640, Adjective 436, PronominalVerbs 69, ReportedSpeech 31, Spelling 62, WordFormation 6); TenseChoiceAuthor still out; Vocab HELD. Mean expected_buckets 6.27 -> 12.82. Merge each on return, never batch; the tool reads BOTH data/review_packets_tier2/ and returned/.
  - Job 9 GUARDED 2026-08-14: tools/build_ratified_expected_buckets.py -> data/expected_buckets_ratified.json (7,260 pairs); Job 9 unions it in and GATE 0 refuses the run on any drop; missing sidecar = hard stop. Dry run green. Regenerate the sidecar after every merge.
  - HELD: Vocab's 8,677 search findings (7,765 frequency-band claims + 912 root) - my packet offered 180 band buckets out of 183. Two questions on the thread: marking axis or reporting lens, and what a vocabulary search packet should actually ask for.
  - OWED to the 8 search seats: they searched under the WITHDRAWN ancestor rule; packets corrected in place, aggregate top-ups invited. Expect small returns.
  - OWED: StressAuthor's 30 atom labels, then mint the phonology tree. lexical_simple (6,413 items) put back to the seat as a question.
  - OWED: Housing owes the 5 expect_rules predicates + the prompt pass (THE JOB to the top, BREADTH folded in); I owe a ruling on s5.5 once Housing says whether it meant 0.9 or the partial 0.5.
  - OWED to NegationAuthor: five negation leaves (concord, responses, restrictive, pleonastic, correlative) were absent from its packet, so ~15 concord items and the echo/pleonastic items it struck are now untagged for their REAL leaf. It has offered the item lists. Same question likely applies to other seats whose packets carried only some leaves.
  - Tier-2 exclusion RULED 2026-08-13 (Smith: everyone contributed translation items, everyone gets a packet). All 8 cut as SEARCH packets - seat searches the 913 reference translations against its own buckets, rather than judging machine proposals it has no trigger set for.
  - OWED: wire equivalence_class members into the tier-1 vocabulary derivation (a class member must be able to fire, or a correct alternative answer has no bucket).
  - OWED: strip the unfireable classes (1800 orthography.spelling.*, 1012 it_en vocabulary) from expected_buckets per the earned-list ruling; specify the confirmed-set provenance field.
  - OWED: only TenseChoiceAuthor's search packet is still out (28 buckets, 66 translation items of its own - the largest translation topic on the estate and it was excluded from wave 1).
  - OWED: tier-1 expected_buckets backfill on the 15 newer translation items Housing found (914 on disk, 899 derived).
  - OWED: review Architecture_Housing_marker_expectation_suite v1 section 4 (the prompt).
  - Theme axes RULED (Architecture_Vocab_theme_axes.md v1): two navigations not one list; kind field carries both axes; render fix BEFORE the 85 sub-themes; sayability test as the mint gate.
  - Vocabulary deletion forensics DONE 2026-08-13: no systemic loss. Net -29 since May; 128 of my '165 missing' were gender-value changes on loanwords, not deletions. Every deliberate prune is scripted and documented. ONE real casualty: 9 reflexive verbs lost to the 05-26 truncated write, RESTORED 2026-08-14 (18,044 entries). casco/talpa/ampio/importante are the wrong-tag half of duplicate pairs - the correct sibling survives in each case, so no restore is owed.
  - Themes axis: 3 catch-alls hold 59% of the vocabulary (noun_abstract 4920, adjective_quality 3922, verb_action_general 1744). Needs splitting before themes ship as a browsing axis.
  - Vocab judgement needed: ratify sebbene/benche + secco/asciutto from the FILTERED proposals - both screenshot cases are still live. Note secco/asciutto may be 'mutually acceptable answers' rather than one class.
  - DECIDE: gender bucket id has two live forms — authors' `<lemma>.<pos>.gender` (1,062 refs) vs engine's `<lemma>.<pos>.gender.active`. They do not aggregate. Needs a considered migration + behaviour test.
  - Job 1 output needs the tightened gates applied as a FILTER over data/equivalence_class_proposals_2026-08.json (do not re-run).
  - Noun-class taxonomy note written to Vocab (2026-08-05). Housing rider: one axis per panel. Check whether the gender drill can serve the 644 ambiguous loanwords - if it can, live bug.
  - WORK_ORDER_2026-08-05_mechanical_passes.md: jobs 1,2,3,5 ASSIGNED TO QODERWORK 2026-08-05 (live-folder access; run order 3-1-2-5, job 5 gated on job 2 passing); job 4 stays with Housing. On their return: verify against each job's stated acceptance tests before believing any count, then ratify job 1's class proposals with Vocab.
  - OWED: tier-2 equivalence-class residue spec (abbreviation/full-form pairs; Vocab runs tier 1)
  - OWED (new, large): tier-1 expected_buckets deriver over 899 translation items, then the tier-2 morph-it tagger + per-bucket review packets (crosstopic v24)
  - OWED: sweep the 129 gender/article_form references for bare ids on multi-entry lemmas (authoring rule issued 08-05; gemello + mobile currently resolve to the ADJECTIVE entry)
  - Selection policy: OWED on request — the weakness-panel row taxonomy + learner-facing English (§10). Also available for Housing's two open calls (gender-drill identity; where the picker lives).
  - Jargon execution once Housing answers the flat-panel question (PP ruling made; Classes 1-2 mechanics ready)
  - CEFR harvest + Smith-decisions routing; pulse-sheet attempt via Drive connector
  - Chase: 7 answer-leak reacts; AdjectiveAuthor's item-shape react (cue-notation now discharged); cue-notation owes: Possessive 29 / Piacere 12 / Adverb 7
  - PronominalVerbs packet STAMPED/CLOSED (one seat touch remains: instruction-pin + celho_02 flag)
notes:
  - AdjectiveAuthor has one question: is vocab_bucket_references_adjective_agreement.json (99 refs) meant to be in the manifest? It is not.
  - MisconceptionAnalyst is now FIREABLE: splits + pairs + seven families + 82 tag-lists all on its threads.
  - PiacereAuthor has one small job (verb_identity field) — fire with the Analyst or fold into its next wake.
  - AccentAuthor reviews the regenerated batch; Housing confirms a-b-c. Board otherwise as chat-reported.
