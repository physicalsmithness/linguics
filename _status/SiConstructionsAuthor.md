seat: SiConstructionsAuthor
classes: [all-seats, all-authors]
  # Ratified. binds:all-authors RETROFITS run CENTRALLY — don't self-run; assume Architecture runs them unless a thread names me.
project: Linguics
updated: 2026-08-12
waiting: closed
needs_from_smith: none
blocked_by:
claude_can_verify: yes — returned packet on disk, reparsed, every row has a verdict
summary: Tier-2 review packet reviewed and RETURNED (10 keep / 15 strike / 4 additions). All my threads resolved; merge is Architecture's. Nothing owed to Smith.
queue: []
done_this_wave:
  - Tier-2 impersonal.basic packet — 25 rows judged + 4 additions, returned to data/review_packets_tier2/returned/; thread v2. Merge is Architecture's (do not wait).
open_elsewhere_not_mine:
  - Architecture merges the returned packet on its next wake.
  - impersonal.ci_si leaf still proposed, unratified (not blocking).
  - Struck passivante/reflexive/advanced rows flagged as seeds for those buckets' own tier-2 packets.
